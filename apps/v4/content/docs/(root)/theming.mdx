---
title: テーマ
description: CSS 変数とテーマトークンの使用。
---

<Callout>

テーマを視覚的に構築したいですか? [shadcn/create](/create) を使用して色、半径、フォント、アイコンをプレビューし、プロジェクトのプリセットを生成します。

</Callout>

テーマには CSS 変数を使用し、推奨します。

これにより、コンポーネントがデフォルトで使用する `background`、`foreground`、`primary` などのセマンティックテーマトークンが得られます。CSS でこれらのトークンをオーバーライドすると、コンポーネントクラスを書き換えずにアプリの外観を変更できます。

```tsx /bg-background/ /text-foreground/
<div className="bg-background text-foreground" />
```

テーマに CSS 変数を使用するには、`components.json` ファイルで `tailwind.cssVariables` を `true` に設定します。これがデフォルトです。

```json {8} title="components.json" showLineNumbers
{
  "style": "base-nova",
  "rsc": true,
  "tailwind": {
    "config": "",
    "css": "app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true
  }
}
```

Tailwind は、これらのトークンを `bg-background`、`text-foreground`、`border-border`、`ring-ring` などのユーティリティにマップします。

ダークモードは、`.dark` セレクター内の同じトークンをオーバーライドすることで機能します。テーマプロバイダーの追加と `.dark` クラスの切り替えについては、[ダークモードドキュメント](/docs/dark-mode/next) を参照してください。

## トークン規約

セマンティックな背景と前景のペアを使用します。ベーストークンは表面の色を制御し、`-foreground` トークンはその表面上にあるテキストとアイコンの色を制御します。

<Callout className="mt-4">

表面トークンでは背景のサフィックスが省略されます。たとえば、`primary` は `primary-foreground` とペアになります。

</Callout>

次の CSS 変数があるとします。

```css
--primary: oklch(0.205 0 0);
--primary-foreground: oklch(0.985 0 0);
```

次のコンポーネントの `background` の色は `var(--primary)` になり、`foreground` の色は `var(--primary-foreground)` になります。

```tsx
<div className="bg-primary text-primary-foreground">Hello</div>
```

## テーマトークン

これらのトークンは、CSS ファイルの `:root` および `.dark` の下に存在します。

|トークン |何を制御するのか |使用者 |
| ------------------------------------------------ | ------------------------------------------------------ | ---------------------------------------------------------------------------- |
| `background` / `foreground` |デフォルトのアプリの背景とテキストの色。             |ページシェル、ページセクション、およびデフォルトのテキスト。                             |
| `card` / `card-foreground` |隆起した表面とその内部のコンテンツ。         | `Card`、ダッシュボードパネル、設定パネル。                                   |
| `popover` / `popover-foreground` |浮遊した表面とその内部のコンテンツ。         | `Popover`、`DropdownMenu`、`ContextMenu`、およびその他のオーバーレイ。                |
| `primary` / `primary-foreground` |強調されたアクションとブランドの表面。              |デフォルトの `Button`、選択された状態、バッジ、およびアクティブなアクセント。               |
| `secondary` / `secondary-foreground` |低強調のアクションと埋め込み型のサポート面。 |二次ボタン、二次バッジ、およびサポート UI。                      |
| `muted` / `muted-foreground` |控えめな表面と、強調度の低いコンテンツ。            |説明、プレースホルダー、空の状態、ヘルパーテキスト、および落ち着いた表面。 |
| `accent` / `accent-foreground` |インタラクティブなホバー、フォーカス、アクティブな状態の表面。         |ゴーストボタン、メニューのハイライト状態、ホバーされた行、および選択された項目。      |
| `destructive` |破壊的なアクションとエラーの強調。                |破壊的なボタン、無効な状態、および破壊的なメニュー項目。             |
| `border` |デフォルトの境界線と区切り文字。                        |カード、メニュー、テーブル、セパレーター、レイアウトの仕切り。                       |
| `input` |フォームコントロールの境界線と入力表面処理。      | `Input`、`Textarea`、`Select`、およびアウトラインスタイルのコントロール。                   |
| `ring` |フォーカスリングと輪郭。                              |ボタン、入力、チェックボックス、メニュー、およびその他のフォーカス可能なコントロール。            |
| `chart-1` ... `chart-5` |デフォルトのグラフパレット。                             |チャートとチャート駆動のダッシュボードブロック。                                    |
| `sidebar` / `sidebar-foreground` |ベースのサイドバーの表面とデフォルトのサイドバーのテキスト。     | `Sidebar` コンテナーとそのデフォルトのコンテンツ。                             |
| `sidebar-primary` / `sidebar-primary-foreground` |サイドバー内のアクションを強調します。              |アクティブなアイテム、アイコンタイル、バッジ、サイドバー CTA。                          |
| `sidebar-accent` / `sidebar-accent-foreground` |サイドバー内のホバーおよび選択状態。          |サイドバー メニューのホバー状態、開いている項目、およびインタラクティブな行。                 |
| `sidebar-border` |サイドバー固有の境界線と区切り文字。               |サイドバーのヘッダー、グループ、および内部の仕切り。                              |
| `sidebar-ring` |サイドバー固有のフォーカスリング。                          |サイドバー内のフォーカスされたコントロール。                                         |
| `radius` |ベースコーナー半径のスケール。                          |カード、入力、ボタン、ポップオーバー、および派生した `radius-*` トークン。         |

<Callout className="mt-4">

チャートトークンについては、[チャートテーマに関するドキュメント](/docs/components/chart#theming) で詳しく説明されています。

</Callout>

## 半径スケール

`--radius` は、テーマの基本半径トークンです。

そこから小さな半径スケールを導き出すので、コンポーネントは単一の真実の情報源を共有しながら、一貫した角のサイズを使用できます。

```css title="app/globals.css" showLineNumbers
@theme inline {
  --radius-sm: calc(var(--radius) * 0.6);
  --radius-md: calc(var(--radius) * 0.8);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) * 1.4);
  --radius-2xl: calc(var(--radius) * 1.8);
  --radius-3xl: calc(var(--radius) * 2.2);
  --radius-4xl: calc(var(--radius) * 2.6);
}
```

つまり:

- `radius-lg` は基本値です。
- `--radius` を基準に、小さい半径へスケールダウンします。
- `--radius` を基準に、より大きな半径へスケールアップします。
- `--radius` を変更すると、半径スケール全体が更新されます。

## 新しいトークンの追加

新しいトークンを追加するには、`:root` および `.dark` で定義し、`@theme inline` を使用して Tailwind に公開します。

```css title="app/globals.css" showLineNumbers
:root {
  --warning: oklch(0.84 0.16 84);
  --warning-foreground: oklch(0.28 0.07 46);
}

.dark {
  --warning: oklch(0.41 0.11 46);
  --warning-foreground: oklch(0.99 0.02 95);
}

@theme inline {
  --color-warning: var(--warning);
  --color-warning-foreground: var(--warning-foreground);
}
```

コンポーネントで `bg-warning` および `text-warning-foreground` を使用できるようになりました。

```tsx /bg-warning/ /text-warning-foreground/
<div className="bg-warning text-warning-foreground" />
```

## ベースカラー

`tailwind.baseColor` は、`init` を実行するとき、またはプリセットを使用するときにプロジェクトに対して生成されるデフォルトのトークン値を制御します。

利用可能なベースカラーは次のとおりです: **ニュートラル**、**ストーン**、**ジンク**、**モーブ**、**オリーブ**、**ミスト**、**トープ**。

## デフォルトのテーマ CSS

以下は完全なデフォルトの `neutral` テーマスキャフォールドです。これをグローバル CSS ファイルにコピーし、必要に応じてトークンを調整します。

<CodeCollapsibleWrapper>

```css showLineNumbers title="app/globals.css"
@import "tailwindcss";
@import "shadcn/tailwind.css";

@custom-variant dark (&:is(.dark *));

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-border: var(--border);
  --color-input: var(--input);
  --color-ring: var(--ring);
  --color-chart-1: var(--chart-1);
  --color-chart-2: var(--chart-2);
  --color-chart-3: var(--chart-3);
  --color-chart-4: var(--chart-4);
  --color-chart-5: var(--chart-5);
  --color-sidebar: var(--sidebar);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-ring: var(--sidebar-ring);
  --radius-sm: calc(var(--radius) * 0.6);
  --radius-md: calc(var(--radius) * 0.8);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) * 1.4);
  --radius-2xl: calc(var(--radius) * 1.8);
  --radius-3xl: calc(var(--radius) * 2.2);
  --radius-4xl: calc(var(--radius) * 2.6);
}

:root {
  --radius: 0.625rem;
  --background: oklch(1 0 0);
  --foreground: oklch(0.145 0 0);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.145 0 0);
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.145 0 0);
  --primary: oklch(0.205 0 0);
  --primary-foreground: oklch(0.985 0 0);
  --secondary: oklch(0.97 0 0);
  --secondary-foreground: oklch(0.205 0 0);
  --muted: oklch(0.97 0 0);
  --muted-foreground: oklch(0.556 0 0);
  --accent: oklch(0.97 0 0);
  --accent-foreground: oklch(0.205 0 0);
  --destructive: oklch(0.577 0.245 27.325);
  --border: oklch(0.922 0 0);
  --input: oklch(0.922 0 0);
  --ring: oklch(0.708 0 0);
  --chart-1: oklch(0.646 0.222 41.116);
  --chart-2: oklch(0.6 0.118 184.704);
  --chart-3: oklch(0.398 0.07 227.392);
  --chart-4: oklch(0.828 0.189 84.429);
  --chart-5: oklch(0.769 0.188 70.08);
  --sidebar: oklch(0.985 0 0);
  --sidebar-foreground: oklch(0.145 0 0);
  --sidebar-primary: oklch(0.205 0 0);
  --sidebar-primary-foreground: oklch(0.985 0 0);
  --sidebar-accent: oklch(0.97 0 0);
  --sidebar-accent-foreground: oklch(0.205 0 0);
  --sidebar-border: oklch(0.922 0 0);
  --sidebar-ring: oklch(0.708 0 0);
}

.dark {
  --background: oklch(0.145 0 0);
  --foreground: oklch(0.985 0 0);
  --card: oklch(0.205 0 0);
  --card-foreground: oklch(0.985 0 0);
  --popover: oklch(0.205 0 0);
  --popover-foreground: oklch(0.985 0 0);
  --primary: oklch(0.922 0 0);
  --primary-foreground: oklch(0.205 0 0);
  --secondary: oklch(0.269 0 0);
  --secondary-foreground: oklch(0.985 0 0);
  --muted: oklch(0.269 0 0);
  --muted-foreground: oklch(0.708 0 0);
  --accent: oklch(0.269 0 0);
  --accent-foreground: oklch(0.985 0 0);
  --destructive: oklch(0.704 0.191 22.216);
  --border: oklch(1 0 0 / 10%);
  --input: oklch(1 0 0 / 15%);
  --ring: oklch(0.556 0 0);
  --chart-1: oklch(0.488 0.243 264.376);
  --chart-2: oklch(0.696 0.17 162.48);
  --chart-3: oklch(0.769 0.188 70.08);
  --chart-4: oklch(0.627 0.265 303.9);
  --chart-5: oklch(0.645 0.246 16.439);
  --sidebar: oklch(0.205 0 0);
  --sidebar-foreground: oklch(0.985 0 0);
  --sidebar-primary: oklch(0.488 0.243 264.376);
  --sidebar-primary-foreground: oklch(0.985 0 0);
  --sidebar-accent: oklch(0.269 0 0);
  --sidebar-accent-foreground: oklch(0.985 0 0);
  --sidebar-border: oklch(1 0 0 / 10%);
  --sidebar-ring: oklch(0.556 0 0);
}

@layer base {
  * {
    @apply border-border outline-ring/50;
  }

  body {
    @apply bg-background text-foreground;
  }
}
```

</CodeCollapsibleWrapper>

## CSS 変数なし

CSS 変数を使用したくない場合は、代わりに CLI でインライン Tailwind カラー ユーティリティを使用してコンポーネントを生成できます。

```bash
npx shadcn@latest init --no-css-variables
```

これにより、`components.json` ファイル内の `tailwind.cssVariables` が `false` に設定されます。

```tsx /bg-zinc-950/ /text-zinc-50/ /dark:bg-white/ /dark:text-zinc-950/
<div className="bg-zinc-950 text-zinc-50 dark:bg-white dark:text-zinc-950" />
```

<Callout className="mt-4">

これはインストール時の選択です。既存のプロジェクトを切り替えるには、コンポーネントを削除して再インストールします。

</Callout>
