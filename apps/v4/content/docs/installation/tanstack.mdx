---
title: TanStack
description: TanStack Start 用に shadcn/ui をインストールして構成します。
---

開始点と一致するセットアップを選択します。

<div className="mt-6 grid gap-4 sm:grid-cols-3 sm:gap-6">
  <LinkedCard
    href="#scaffold-with-create"
    className="items-start gap-1 p-6 text-sm md:p-6"
  >
    <div className="font-medium">Use shadcn/create</div>
    <div className="leading-relaxed text-muted-foreground">
      プリセットを構築し、TanStack プロジェクト コマンドを生成します。
    </div>
  </LinkedCard>
  <LinkedCard
    href="#scaffold-with-cli"
    className="items-start gap-1 p-6 text-sm md:p-6"
  >
    <div className="font-medium">Use the CLI</div>
    <div className="leading-relaxed text-muted-foreground">
      ターミナルから新しい TanStack プロジェクトをスキャフォールディングします。
    </div>
  </LinkedCard>
  <LinkedCard
    href="#existing-start-project"
    className="items-start gap-1 p-6 text-sm md:p-6"
  >
    <div className="font-medium">Existing Project</div>
    <div className="leading-relaxed text-muted-foreground">
      既存の TanStack プロジェクトで shadcn/ui を手動で構成します。
    </div>
  </LinkedCard>
</div>

<div id="scaffold-with-create" className="scroll-mt-24" />
## shadcn/create

<Steps>

### プリセットのビルド

[shadcn/create](/create?template=start) を開いて、プリセットを視覚的に構築します。スタイル、色、フォント、アイコンなどを選択します。

<Button asChild size="sm">
  <Link
    href="/create?template=start"
    target="_blank"
    rel="noopener noreferrer"
    className="mt-6 no-underline!"
  >
    shadcn/create
  </Link>
</Button>

### プロジェクトの作成

`Create Project` をクリックし、パッケージ マネージャーを選択し、生成されたコマンドをコピーします。

生成されるコマンドは次のようになります。

```bash
npx shadcn@latest init --preset [CODE] --template start
```

正確なコマンドには、`--base`、`--monorepo`、`--rtl` などの選択したオプションが含まれます。

### コンポーネントの追加

`Card` コンポーネントをプロジェクトに追加します。

```bash
npx shadcn@latest add card
```

モノレポを作成した場合は `apps/web` からコマンドを実行するか、リポジトリのルートからワークスペースを指定します。

```bash
npx shadcn@latest add card -c apps/web
```

上記のコマンドは、`Card` コンポーネントをプロジェクトに追加します。次に、次のようにインポートできます:

```tsx showLineNumbers title="src/routes/index.tsx"
import { createFileRoute } from "@tanstack/react-router"

import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card"

export const Route = createFileRoute("/")({
  component: App,
})

function App() {
  return (
    <Card className="max-w-sm">
      <CardHeader>
        <CardTitle>Project Overview</CardTitle>
        <CardDescription>
          Track progress and recent activity for your TanStack Start app.
        </CardDescription>
      </CardHeader>
      <CardContent>
        Your design system is ready. Start building your next component.
      </CardContent>
    </Card>
  )
}
```

モノレポを作成した場合は `apps/web/src/routes/index.tsx` を更新し、代わりに `@workspace/ui/components/card` からインポートします。

</Steps>

<div id="scaffold-with-cli" className="scroll-mt-24" />
## CLI

<Steps>

### プロジェクトの作成

`init` コマンドを実行して、新しい TanStack Start プロジェクトをスキャフォールディングします。プロンプトに従って、ベース、プリセット、モノレポなどのプロジェクトを構成します。

```bash
npx shadcn@latest init -t start
```

**モノレポ プロジェクトの場合は、`--monorepo` フラグを使用します。**

```bash
npx shadcn@latest init -t start --monorepo
```

### コンポーネントの追加

`Card` コンポーネントをプロジェクトに追加します。

```bash
npx shadcn@latest add card
```

モノレポを作成した場合は `apps/web` からコマンドを実行するか、リポジトリのルートからワークスペースを指定します。

```bash
npx shadcn@latest add card -c apps/web
```

上記のコマンドは、`Card` コンポーネントをプロジェクトに追加します。次に、次のようにインポートできます:

```tsx showLineNumbers title="src/routes/index.tsx"
import { createFileRoute } from "@tanstack/react-router"

import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card"

export const Route = createFileRoute("/")({
  component: App,
})

function App() {
  return (
    <Card className="max-w-sm">
      <CardHeader>
        <CardTitle>Project Overview</CardTitle>
        <CardDescription>
          Track progress and recent activity for your TanStack Start app.
        </CardDescription>
      </CardHeader>
      <CardContent>
        Your design system is ready. Start building your next component.
      </CardContent>
    </Card>
  )
}
```

モノレポを作成した場合は `apps/web/src/routes/index.tsx` を更新し、代わりに `@workspace/ui/components/card` からインポートします。

</Steps>

<div id="existing-start-project" className="scroll-mt-24" />
## Existing Project

<Steps>

### プロジェクトの作成

新しい TanStack Start プロジェクトが必要な場合は、先に作成します。既存のプロジェクトがある場合は、この手順を省略してください。

```bash
npx @tanstack/cli@latest create
```

TanStack Start、React フレームワーク、および推奨デフォルトを選択して、Tailwind CSS と `@/*` インポート エイリアスが構成されるようにします。

<Callout className="mt-6">

プロンプトが表示されたら、`shadcn` アドオンを追加しないでください。`shadcn` CLI は、このガイドの後半で shadcn/ui を構成します。

</Callout>

TanStack CLI は、Tailwind CSS とデフォルトの `@/*` インポート エイリアスをすでに構成しています。古いまたはカスタムの TanStack Start アプリに shadcn/ui を追加する場合は、続行する前に両方が構成されていることを確認してください。

### CLI を実行します

`shadcn` init コマンドを実行して、プロジェクトに shadcn/ui をセットアップします。

```bash
npx shadcn@latest init
```

### コンポーネントの追加

これで、プロジェクトへのコンポーネントの追加を開始できます。

```bash
npx shadcn@latest add button
```

上記のコマンドは、`Button` コンポーネントをプロジェクトに追加します。次に、次のようにインポートできます:

```tsx showLineNumbers title="src/routes/index.tsx"
import { createFileRoute } from "@tanstack/react-router"

import { Button } from "@/components/ui/button"

export const Route = createFileRoute("/")({
  component: App,
})

function App() {
  return (
    <div className="flex min-h-svh items-center justify-center p-6">
      <Button>Click me</Button>
    </div>
  )
}
```

</Steps>
