---
title: Laravel
description: Laravel 用に shadcn/ui をインストールして設定します。
---

shadcn CLI は、新しい Laravel アプリをスキャフォールディングしません。まず、React スターター キットを使用して Laravel アプリを作成し、次に shadcn/ui を構成する方法を選択します。

<Steps>

### プロジェクトの作成

Laravel インストーラーを使用して新しい Laravel アプリを作成します。

```bash
laravel new my-app
```

React と Inertia がすでに構成された Laravel アプリがある場合は、この手順を省略してください。

プロンプトが表示されたら、**React** スターター キットを選択します。詳細については、公式の [Laravel フロントエンド ドキュメント](https://laravel.com/docs/12.x/frontend) を参照してください。

次に、プロジェクト ディレクトリに移動します:

```bash
cd my-app
```

</Steps>

<div className="mt-10 grid gap-4 sm:grid-cols-2 sm:gap-6">
  <LinkedCard
    href="#configure-with-create"
    className="items-start gap-1 p-6 text-sm md:p-6"
  >
    <div className="font-medium">shadcn/create</div>
    <div className="leading-relaxed text-muted-foreground">
      プリセットを視覚的に構築し、Laravel init コマンドを生成します。
    </div>
  </LinkedCard>
  <LinkedCard
    href="#configure-with-cli"
    className="items-start gap-1 p-6 text-sm md:p-6"
  >
    <div className="font-medium">Use the CLI</div>
    <div className="leading-relaxed text-muted-foreground">
      ターミナルから Laravel アプリで shadcn/ui を直接設定します。
    </div>
  </LinkedCard>
</div>

<div id="configure-with-create" className="scroll-mt-24" />
## shadcn/create

<Steps>

### プリセットを構築する

[shadcn/create](/create?template=laravel) を開いてプリセットを視覚的に構築します。スタイル、色、フォント、アイコンなどを選択します。

<Button asChild size="sm">
  <Link
    href="/create?template=laravel"
    target="_blank"
    rel="noopener noreferrer"
    className="mt-6 no-underline!"
  >
    shadcn/create
  </Link>
</Button>

### コマンドを実行する

`Create Project` をクリックし、パッケージ マネージャーを選択し、生成されたコマンドをコピーします。

生成されるコマンドは次のようになります。

```bash
npx shadcn@latest init --preset [CODE] --template laravel
```

Laravel アプリのルートからコマンドを実行します。

`components.json` とコンポーネントを上書きするように求められたら、`Yes` を選択します。

### コンポーネントの追加

`Switch` コンポーネントをプロジェクトに追加します。

```bash
npx shadcn@latest add switch
```

上記のコマンドは、`Switch` コンポーネントを `resources/js/components/ui/switch.tsx` に追加します。次に、次のようにインポートできます。

```tsx title="resources/js/pages/index.tsx" showLineNumbers {1,6}
import { Switch } from "@/components/ui/switch"

const MyPage = () => {
  return (
    <div>
      <Switch />
    </div>
  )
}

export default MyPage
```

</Steps>

<div id="configure-with-cli" className="scroll-mt-24" />
## Use the CLI

<Steps>

### CLI を実行します

Laravel アプリのルートから `shadcn` init コマンドを実行します。

```bash
npx shadcn@latest init
```

`components.json` とコンポーネントを上書きするように求められたら、`Yes` を選択します。

### コンポーネントの追加

`Switch` コンポーネントをプロジェクトに追加します。

```bash
npx shadcn@latest add switch
```

上記のコマンドは、`Switch` コンポーネントを `resources/js/components/ui/switch.tsx` に追加します。次に、次のようにインポートできます。

```tsx title="resources/js/pages/index.tsx" showLineNumbers {1,6}
import { Switch } from "@/components/ui/switch"

const MyPage = () => {
  return (
    <div>
      <Switch />
    </div>
  )
}

export default MyPage
```

</Steps>
