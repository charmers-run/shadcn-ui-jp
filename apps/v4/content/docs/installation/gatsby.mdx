---
title: Gatsby
description: Gatsby 用に shadcn/ui をインストールして構成します。
---

<Callout className="mb-6 border-blue-600 bg-blue-50 dark:border-blue-900 dark:bg-blue-950 [&_code]:bg-blue-100 dark:[&_code]:bg-blue-900">
  **注:** このガイドは Tailwind CSS v3 を使用する Gatsby 用です。新しいプロジェクトでは、Tailwind
  CSS v4 をサポートする他のフレームワークの使用をお勧めします。
</Callout>

<Steps>

### プロジェクトの作成

まず、`create-gatsby` を使用して新しい Gatsby プロジェクトを作成します。

```bash
npm init gatsby
```

### TypeScript と Tailwind CSS を使用するように Gatsby プロジェクトを構成する

プロジェクトを構成するため、いくつかの質問が表示されます:

```txt showLineNumbers
✔ What would you like to call your site?
· your-app-name
✔ What would you like to name the folder where your site will be created?
· your-app-name
✔ Will you be using JavaScript or TypeScript?
· TypeScript
✔ Will you be using a CMS?
· Choose whatever you want
✔ Would you like to install a styling system?
· Tailwind CSS
✔ Would you like to install additional features with other plugins?
· Choose whatever you want
✔ Shall we do this? (Y/n) · Yes
```

### tsconfig.json ファイルを編集する

パスを解決するため、次のコードを `tsconfig.json` ファイルに追加します。

```ts {4-9} showLineNumbers
{
  "compilerOptions": {
    // ...
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./src/*"
      ]
    }
    // ...
  }
}
```

### gatsby-node.ts ファイルの作成

プロジェクトのルートに `gatsby-node.ts` ファイルがまだ存在しない場合は作成し、アプリがパスを解決できるように、以下のコードを `gatsby-node` ファイルに追加します。

```ts
import * as path from "path"

export const onCreateWebpackConfig = ({ actions }) => {
  actions.setWebpackConfig({
    resolve: {
      alias: {
        "@/components": path.resolve(__dirname, "src/components"),
        "@/lib/utils": path.resolve(__dirname, "src/lib/utils"),
      },
    },
  })
}
```

### CLI を実行します

`shadcn` init コマンドを実行して、プロジェクトをセットアップします。

```bash
npx shadcn@latest init
```

### 以上です

これで、プロジェクトへのコンポーネントの追加を開始できます。

```bash
npx shadcn@latest add button
```

上記のコマンドは、`Button` コンポーネントをプロジェクトに追加します。次に、次のようにインポートできます。

```tsx {1,6} showLineNumbers
import { Button } from "@/components/ui/button"

export default function Home() {
  return (
    <div>
      <Button>Click me</Button>
    </div>
  )
}
```

</Steps>
