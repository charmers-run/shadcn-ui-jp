---
title: 2026 年 4 月 - 部分的なプリセットの適用
description: 既存のコンポーネントを保持したまま、プリセットのテーマまたはフォントのみを適用します。
date: 2026-04-22
---

プリセットを選択的に適用できるようになりました。

誰かがプリセットをあなたと共有していて、すでに自分のコンポーネントを持っているものの、テーマやフォントは気に入っているとします。その場合、それだけを適用できます。

コンポーネントはそのまま保持されます。必要なものだけを適用してください。

```bash
# Apply the full preset.
npx shadcn@latest apply --preset b2D0vQ7G4

# Apply only the theme.
npx shadcn@latest apply --preset b2D0vQ7G4 --only theme

# Apply only the fonts.
npx shadcn@latest apply --preset b2D0vQ7G4 --only font

# Apply theme and fonts.
npx shadcn@latest apply --preset b2D0vQ7G4 --only theme,font
```

デフォルトの動作は変更されません。`shadcn apply --preset <preset>` を実行しても、完全なプリセットが適用されます。

部分プリセット適用は現在、`theme` および `font` をサポートしています。

<Button asChild size="sm">
  <Link href="/create" className="mt-6 no-underline!">
    プリセットを試してみる
  </Link>
</Button>
