---
title: 2025 年 10 月 - 新しいコンポーネント
description: スピナー、Kbd、ボタングループ、入力グループ、フィールド、項目、および空のコンポーネント。
date: 2025-10-03
---

このコンポーネントのラウンドでは、私たちが毎日構築しているもの、何度も再構築している退屈なものを検討し、実際に使用できる再利用可能な抽象化を作成しました。

**これらのコンポーネントは、Radix、Base UI、React Aria など、あらゆるコンポーネント ライブラリで動作します。コピーしてプロジェクトに貼り付けます。**

- [スピナー](#spinner): 読み込み状態を示すインジケーターです。
- [Kbd](#kbd): キーボードのキーまたはキーのグループを表示します。
- [ボタングループ](#button-group): アクションボタンや分割ボタンをまとめるグループです。
- [入力グループ](#input-group): アイコンやボタン、ラベルなどを入力欄に追加できます。
- [フィールド](#field): 1 つのコンポーネント。すべてのフォーム。
- [アイテム](#item): アイテムやカードなどのリストを表示します。
- [空](#empty): 空の状態に使用します。

### スピナー

それでは、最も簡単なもの、**スピナー** と **Kbd** から始めましょう。かなり基本的なものです。私たちは皆、それらが何をするか知っています。

スピナーをレンダリングする方法は次のとおりです。

```tsx
import { Spinner } from "@/components/ui/spinner"
```

```tsx
<Spinner />
```

以下にその様子を示します。

<ComponentPreview styleName="new-york-v4"
  name="spinner-basic"

/>

ボタンでは次のようになります。

<ComponentPreview styleName="new-york-v4"
  name="spinner-button"

/>

コードを編集して、独自のスピナーに置き換えることができます。

<ComponentPreview styleName="new-york-v4"
  name="spinner-custom"

/>

### Kbd

Kbd はキーボードのキーをレンダリングするコンポーネントです。

```tsx
import { Kbd, KbdGroup } from "@/components/ui/kbd"
```

```tsx
<Kbd>Ctrl</Kbd>
```

`KbdGroup` を使用して、キーボードのキーをグループ化します。

```tsx showLineNumbers
<KbdGroup>
  <Kbd>Ctrl</Kbd>
  <Kbd>B</Kbd>
</KbdGroup>
```

<ComponentPreview styleName="new-york-v4"
  name="kbd-demo"

/>

これをボタン、ツールチップ、入力グループなどに追加できます。

### ボタングループ

ボタングループについては、多くのリクエストをいただきました。これは、関連するボタンを一貫したスタイルでグループ化するコンテナです。アクショングループや分割ボタンなどに最適です。

<ComponentPreview styleName="new-york-v4"
  name="button-group-demo"

/>

コードは次のとおりです。

```tsx
import { ButtonGroup } from "@/components/ui/button-group"
```

```tsx showLineNumbers
<ButtonGroup>
  <Button>Button 1</Button>
  <Button>Button 2</Button>
</ButtonGroup>
```

ボタングループをネストして、間隔をあけてより複雑なレイアウトを作成できます。

```tsx showLineNumbers
<ButtonGroup>
  <ButtonGroup>
    <Button>Button 1</Button>
    <Button>Button 2</Button>
  </ButtonGroup>
  <ButtonGroup>
    <Button>Button 3</Button>
    <Button>Button 4</Button>
  </ButtonGroup>
</ButtonGroup>
```

`ButtonGroupSeparator` を使用して分割ボタンを作成します。古典的なドロップダウン パターン。

<ComponentPreview styleName="new-york-v4"
  name="button-group-dropdown"

/>

これを使用して、接頭辞または接尾辞のボタンやテキストを入力に追加することもできます。

<ComponentPreview styleName="new-york-v4"
  name="button-group-select"

/>

```tsx showLineNumbers
<ButtonGroup>
  <ButtonGroupText>Prefix</ButtonGroupText>
  <Input placeholder="Type something here..." />
  <Button>Button</Button>
</ButtonGroup>
```

### 入力グループ

入力グループを使用すると、アイコンやボタンなどを入力に追加できます。ご存知のとおり、入力周りに常に必要となるこれらすべての小さな部分です。

```tsx
import {
  InputGroup,
  InputGroupAddon,
  InputGroupInput,
} from "@/components/ui/input-group"
```

```tsx showLineNumbers
<InputGroup>
  <InputGroupInput placeholder="Search..." />
  <InputGroupAddon>
    <SearchIcon />
  </InputGroupAddon>
</InputGroup>
```

アイコン付きのプレビューは次のとおりです。

<ComponentPreview
  styleName="new-york-v4"
  name="input-group-icon"
  className="[&_.preview]:h-[300px] [&_pre]:h-[300px]!"
/>

入力グループにボタンを追加することもできます。

<ComponentPreview
  styleName="new-york-v4"
  name="input-group-button"
  className="[&_.preview]:h-[300px] [&_pre]:h-[300px]!"
/>

またはテキスト、ラベル、ツールチップなど...

<ComponentPreview
  styleName="new-york-v4"
  name="input-group-text"
  className="[&_.preview]:h-[350px] [&_pre]:h-[350px]!"
/>

また、テキストエリアとも連携できるため、多くのノブやダイヤル、または別のプロンプトフォームを備えた非常に複雑なコンポーネントを構築できます。

<ComponentPreview
  styleName="new-york-v4"
  name="input-group-textarea"
  className="[&_.preview]:h-[450px] [&_pre]:h-[450px]!"
/>

ああ、スピナーを使ったクールなものをいくつか紹介します。

<ComponentPreview
  styleName="new-york-v4"
  name="input-group-spinner"
  className="[&_.preview]:h-[350px] [&_pre]:h-[350px]!"
/>

### フィールド

本当に複雑なフォームを構築するためのコンポーネントである **フィールド** を紹介します。ここの抽象化は美しいです。

正しく理解するまでに長い時間がかかりましたが、Server Actions、React Hook Form、TanStack Form、Bring Your Own Form など、すべてのフォーム ライブラリで動作するようにしました。

```tsx
import {
  Field,
  FieldDescription,
  FieldError,
  FieldLabel,
} from "@/components/ui/field"
```

以下に入力を含む基本的なフィールドを示します。

```tsx showLineNumbers
<Field>
  <FieldLabel htmlFor="username">Username</FieldLabel>
  <Input id="username" placeholder="Max Leiter" />
  <FieldDescription>
    Choose a unique username for your account.
  </FieldDescription>
</Field>
```

<ComponentPreview
  styleName="new-york-v4"
  name="field-input"
  className="[&_.preview]:h-[350px] [&_pre]:h-[350px]!"
/>

すべてのフォーム コントロールで動作します。入力、テキストエリア、選択、チェックボックス、ラジオ、スイッチ、スライダーなど、何でもあります。完全な例を次に示します。

<ComponentPreview
  styleName="new-york-v4"
  name="field-demo"
  className="[&_.preview]:h-[850px] [&_pre]:h-[850px]!"
/>

いくつかのチェックボックスフィールドを次に示します。

<ComponentPreview
  styleName="new-york-v4"
  name="field-checkbox"
  className="[&_.preview]:h-[500px] [&_pre]:h-[500px]!"
/>

`FieldGroup` および `FieldSet` を使用してフィールドをグループ化できます。複数セクションのフォームに最適です。

```tsx showLineNumbers
<FieldSet>
  <FieldLegend />
  <FieldGroup>
    <Field />
    <Field />
  </FieldGroup>
</FieldSet>
```

<ComponentPreview
  styleName="new-york-v4"
  name="field-fieldset"
  className="[&_.preview]:h-[500px] [&_pre]:h-[500px]!"
/>

レスポンシブにするのは簡単です。`orientation="responsive"` を使用すると、コンテナの幅に基づいて垂直レイアウトと水平レイアウトが自動的に切り替わります。それだけです。

<ComponentPreview
  styleName="new-york-v4"
  name="field-responsive"
  className="[&_.preview]:h-[600px] [&_pre]:h-[600px]!"
/>

待ってください。さらに続きがあります。フィールドを `FieldLabel` でラップして、選択可能なフィールド グループを作成します。本当に簡単です。見た目も素晴らしいです。

<ComponentPreview
  styleName="new-york-v4"
  name="field-choice-card"
  className="[&_.preview]:h-[600px] [&_pre]:h-[600px]!"
/>

### アイテム

これは、ほぼすべての種類のコンテンツを格納できる単純なフレックス コンテナです。

これを何度も構築したので、コンポーネントを作成することにしました。今ではいつも使っています。アイテムやカードなどのリストを表示するために使用します。

```tsx
import {
  Item,
  ItemContent,
  ItemDescription,
  ItemMedia,
  ItemTitle,
} from "@/components/ui/item"
```

基本的な項目は次のとおりです。

```tsx showLineNumbers
<Item>
  <ItemMedia variant="icon">
    <HomeIcon />
  </ItemMedia>
  <ItemContent>
    <ItemTitle>Dashboard</ItemTitle>
    <ItemDescription>Overview of your account and activity.</ItemDescription>
  </ItemContent>
</Item>
```

<ComponentPreview
  styleName="new-york-v4"
  name="item-demo"
  className="[&_.preview]:h-[300px] [&_.preview]:p-4 [&_pre]:h-[300px]!"
/>

アイテムにはアイコン、アバター、または画像を追加できます。

<ComponentPreview
  styleName="new-york-v4"
  name="item-icon"
  className="[&_.preview]:h-[300px] [&_.preview]:p-4 [&_pre]:h-[300px]!"
/>

<ComponentPreview
  styleName="new-york-v4"
  name="item-avatar"
  className="[&_.preview]:h-[300px] [&_.preview]:p-4 [&_pre]:h-[300px]!"
/>

`ItemGroup` を使用した項目のリストは次のようになります。

<ComponentPreview
  styleName="new-york-v4"
  name="item-group"
  className="[&_.preview]:h-[500px] [&_.preview]:p-4 [&_pre]:h-[500px]!"
/>

リンクとして必要ですか? `asChild` プロパティを使用します。

```tsx showLineNumbers
<Item asChild>
  <a href="/dashboard">
    <ItemMedia variant="icon">
      <HomeIcon />
    </ItemMedia>
    <ItemContent>
      <ItemTitle>Dashboard</ItemTitle>
      <ItemDescription>Overview of your account and activity.</ItemDescription>
    </ItemContent>
  </a>
</Item>
```

<ComponentPreview
  styleName="new-york-v4"
  name="item-link"
  className="[&_.preview]:h-[400px] [&_.preview]:p-4 [&_pre]:h-[400px]!"
/>

### 空

さて、最後のもの: **空**。これを使用して、アプリ内に空の状態を表示します。

```tsx
import {
  Empty,
  EmptyContent,
  EmptyDescription,
  EmptyMedia,
  EmptyTitle,
} from "@/components/ui/empty"
```

使い方は次のとおりです。

```tsx showLineNumbers
<Empty>
  <EmptyMedia variant="icon">
    <InboxIcon />
  </EmptyMedia>
  <EmptyTitle>No messages</EmptyTitle>
  <EmptyDescription>You don't have any messages yet.</EmptyDescription>
  <EmptyContent>
    <Button>Send a message</Button>
  </EmptyContent>
</Empty>
```

<ComponentPreview
  styleName="new-york-v4"
  name="empty-demo"
  className="[&_.preview]:h-[400px] [&_.preview]:p-4 [&_pre]:h-[400px]!"
/>

アバターと一緒に使用できます。

<ComponentPreview
  styleName="new-york-v4"
  name="empty-avatar"
  className="[&_.preview]:h-[400px] [&_pre]:h-[400px]!"
/>

または、検索結果やメール購読などの入力グループを使用します。

<ComponentPreview
  styleName="new-york-v4"
  name="empty-input-group"
  className="[&_.preview]:h-[450px] [&_pre]:h-[450px]!"
/>

それでおしまい。 7 つの新しいコンポーネント。すべてのライブラリで動作します。プロジェクトの準備は完了です。
