---
title: 2026 年 5 月 - パッケージのインポートとターゲット エイリアス
description: package.json インポートとポータブル レジストリ ターゲット エイリアスを使用して shadcn/ui を構成します。
date: 2026-05-05
---

`shadcn@4.7.0` の `files.target` でのパッケージのインポートとエイリアスのサポートを追加しました。

## パッケージのインポート

`shadcn` CLI は、コンポーネントのインストール用に `package.json#imports` をサポートするようになりました。インポートの書き換えやサードパーティのレジストリ解決にも利用され、プライベートなインポートにも対応しています。`tsconfig.json` の `compilerOptions.paths` にある `#...` のみに依存するのではなく、`package.json` からエイリアスをインポートできます。

```json title="package.json" showLineNumbers
{
  "imports": {
    "#components/*": "./src/components/*.tsx",
    "#lib/*": "./src/lib/*.ts",
    "#hooks/*": "./src/hooks/*.ts"
  }
}
```

次に、`components.json` で同じルートを使用します。

```json title="components.json" showLineNumbers
{
  "aliases": {
    "components": "#components",
    "ui": "#components/ui",
    "lib": "#lib",
    "hooks": "#hooks",
    "utils": "#lib/utils"
  }
}
```

これは、アプリのローカル ファイルがパッケージ インポートを使用するモノリポジトリでも機能します。
共有 UI ファイルは、ワークスペース パッケージのエクスポートからインポートされます。

セットアップの詳細については、[パッケージ インポート ガイド](/docs/package-imports) を参照してください。

## ターゲットのエイリアス

レジストリ項目は、`files[].target` のターゲットエイリアスを使用して、ユーザーが構成した shadcn ディレクトリ配下にファイルをインストールできるようになりました。たとえば、次のレジストリ項目は、`ui/ai` ディレクトリに `prompt-input.tsx` ファイルをインストールします。

```json title="example.json" showLineNumbers
{
  "files": [
    {
      "path": "registry/default/ai/prompt-input.tsx",
      "type": "registry:ui",
      "target": "@ui/ai/prompt-input.tsx"
    }
  ]
}
```

さらに詳しくは、[レジストリの例](/docs/registry/examples#target-placeholders) を参照してください。
