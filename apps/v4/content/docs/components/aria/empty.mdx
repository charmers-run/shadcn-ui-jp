---
title: 空の状態
description: 空の状態を表示するには、Empty コンポーネントを使用します。
base: aria
component: true
---

<ComponentPreview
  styleName="aria-nova"
  name="empty-demo"
  previewClassName="h-96 p-0"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add empty
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="empty"
  title="components/ui/empty.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx
import {
  Empty,
  EmptyContent,
  EmptyDescription,
  EmptyHeader,
  EmptyMedia,
  EmptyTitle,
} from "@/components/ui/empty"
```

```tsx
<Empty>
  <EmptyHeader>
    <EmptyMedia variant="icon">
      <Icon />
    </EmptyMedia>
    <EmptyTitle>No data</EmptyTitle>
    <EmptyDescription>No data found</EmptyDescription>
  </EmptyHeader>
  <EmptyContent>
    <Button>Add data</Button>
  </EmptyContent>
</Empty>
```

## 構成

`Empty` 状態を構築するには、次の構成を使用します。

```text
Empty
├── EmptyHeader
│   ├── EmptyMedia
│   ├── EmptyTitle
│   └── EmptyDescription
└── EmptyContent
```

## アウトライン

`border` ユーティリティ クラスを使用して、アウトラインの空の状態を作成します。

<ComponentPreview
  styleName="aria-nova"
  name="empty-outline"
  previewClassName="h-96 p-6 md:p-10"
/>

## 背景

`bg-*` および `bg-gradient-*` ユーティリティを使用して、空の状態に背景を追加します。

<ComponentPreview
  styleName="aria-nova"
  name="empty-background"
  previewClassName="h-96 p-0"
/>

## アバター

`EmptyMedia` コンポーネントを使用して、空の状態のアバターを表示します。

<ComponentPreview
  styleName="aria-nova"
  name="empty-avatar"
  previewClassName="h-96 p-0"
/>

## アバター グループ

`EmptyMedia` コンポーネントを使用して、アバター グループを空の状態で表示します。

<ComponentPreview
  styleName="aria-nova"
  name="empty-avatar-group"
  previewClassName="h-96 p-0"
/>

## InputGroup

`InputGroup` コンポーネントを `EmptyContent` コンポーネントに追加できます。

<ComponentPreview
  styleName="aria-nova"
  name="empty-input-group"
  previewClassName="h-96 p-0"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview styleName="aria-nova" name="empty-rtl" direction="rtl" />

## API リファレンス

### Empty

空の状態の主要コンポーネント。`EmptyHeader` コンポーネントと `EmptyContent` コンポーネントをラップします。

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<Empty>
  <EmptyHeader />
  <EmptyContent />
</Empty>
```

### EmptyHeader

`EmptyHeader` コンポーネントは、空のメディア、タイトル、および説明をラップします。

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<EmptyHeader>
  <EmptyMedia />
  <EmptyTitle />
  <EmptyDescription />
</EmptyHeader>
```

### EmptyMedia

アイコンや画像などの空の状態のメディアを表示するには、`EmptyMedia` コンポーネントを使用します。これを使用して、アバターなどの他のコンポーネントを表示することもできます。

| Prop        | Type                  | Default   |
| ----------- | --------------------- | --------- |
| `variant`   | `"default" \| "icon"` | `default` |
| `className` | `string`              |           |

```tsx
<EmptyMedia variant="icon">
  <Icon />
</EmptyMedia>
```

```tsx
<EmptyMedia>
  <Avatar>
    <AvatarImage src="..." />
    <AvatarFallback>CN</AvatarFallback>
  </Avatar>
</EmptyMedia>
```

### EmptyTitle

`EmptyTitle` コンポーネントを使用して、空の状態のタイトルを表示します。

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<EmptyTitle>No data</EmptyTitle>
```

### EmptyDescription

`EmptyDescription` コンポーネントを使用して、空の状態の説明を表示します。

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<EmptyDescription>You do not have any notifications.</EmptyDescription>
```

### EmptyContent

`EmptyContent` コンポーネントを使用して、ボタン、入力、リンクなどの空の状態のコンテンツを表示します。

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<EmptyContent>
  <Button>Add Project</Button>
</EmptyContent>
```
