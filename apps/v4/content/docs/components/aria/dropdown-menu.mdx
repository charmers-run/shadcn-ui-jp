---
title: ドロップダウン メニュー
description: ボタンによってトリガーされる、一連の操作や機能などのメニューをユーザーに表示します。
featured: true
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/Menu
  api: https://react-aria.adobe.com/Menu#api
---

<ComponentPreview
  styleName="aria-nova"
  name="dropdown-menu-demo"
  description="アイコン、ショートカット、サブメニュー項目を含むドロップダウン メニュー。"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add dropdown-menu
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="dropdown-menu"
  title="components/ui/dropdown-menu.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import { Button } from "@/components/ui/button"
import {
  DropdownMenu,
  DropdownMenuGroup,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu"
```

```tsx showLineNumbers
<DropdownMenuTrigger>
  <Button variant="outline">Open</Button>
  <DropdownMenu>
    <DropdownMenuGroup>
      <DropdownMenuLabel>My Account</DropdownMenuLabel>
      <DropdownMenuItem>Profile</DropdownMenuItem>
      <DropdownMenuItem>Billing</DropdownMenuItem>
    </DropdownMenuGroup>
    <DropdownMenuSeparator />
    <DropdownMenuGroup>
      <DropdownMenuItem>Team</DropdownMenuItem>
      <DropdownMenuItem>Subscription</DropdownMenuItem>
    </DropdownMenuGroup>
  </DropdownMenu>
</DropdownMenuTrigger>
```

## 構成

`DropdownMenu` を構築するには、次の構成を使用します。

```text
DropdownMenuTrigger
├── Button
└── DropdownMenu
    ├── DropdownMenuGroup
    │   ├── DropdownMenuLabel
    │   ├── DropdownMenuItem
    │   └── DropdownMenuItem
    ├── DropdownMenuSeparator
    ├── DropdownMenuGroup
    │   ├── DropdownMenuLabel
    │   ├── DropdownMenuItem
    │   └── DropdownMenuItem
    └── DropdownMenuSub
        ├── DropdownMenuSubTrigger
        └── DropdownMenuSubContent
            └── DropdownMenuGroup
                ├── DropdownMenuLabel
                ├── DropdownMenuItem
                └── DropdownMenuItem
```

## 基本

ラベルと区切り文字を含む基本的なドロップダウン メニュー。

<ComponentPreview styleName="aria-nova" name="dropdown-menu-basic" />

## サブメニュー

二次的な操作をネストするには `DropdownMenuSub` を使用します。

<ComponentPreview styleName="aria-nova" name="dropdown-menu-submenu" />

## ショートカット

キーボードのヒントを表示するには `DropdownMenuShortcut` を追加します。

<ComponentPreview styleName="aria-nova" name="dropdown-menu-shortcuts" />

## アイコン

素早く判別できるように、アイコンとラベルを組み合わせます。

<ComponentPreview styleName="aria-nova" name="dropdown-menu-icons" />

## チェックボックス

トグルには `selectionMode="multiple"` を使用します。

<ComponentPreview styleName="aria-nova" name="dropdown-menu-checkboxes" />

## チェックボックス アイコン

チェックボックス項目にアイコンを追加します。

<ComponentPreview styleName="aria-nova" name="dropdown-menu-checkboxes-icons" />

## ラジオグループ

排他的な選択には `selectionMode="single"` を使用します。

<ComponentPreview styleName="aria-nova" name="dropdown-menu-radio-group" />

## ラジオ アイコン

ラジオ オプションをアイコンで表示します。

<ComponentPreview styleName="aria-nova" name="dropdown-menu-radio-icons" />

## 破壊的な操作

不可逆的なアクションには `variant="destructive"` を使用します。

<ComponentPreview styleName="aria-nova" name="dropdown-menu-destructive" />

## アバター

アバターによってトリガーされるアカウント切り替えドロップダウン。

<ComponentPreview styleName="aria-nova" name="dropdown-menu-avatar" />

## 複雑な例

グループ、アイコン、サブメニューを組み合わせた豊富な例。

<ComponentPreview styleName="aria-nova" name="dropdown-menu-complex" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="dropdown-menu-rtl"
  direction="rtl"
/>

## API リファレンス

完全な API リファレンスについては、[React Aria ドキュメント](https://react-aria.adobe.com/Menu) を参照してください。
