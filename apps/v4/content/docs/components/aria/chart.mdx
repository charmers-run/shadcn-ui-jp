---
title: チャート
description: Recharts で構築された美しいチャート。アプリにコピーして貼り付けて使用できます。
base: aria
component: true
---

<Callout>

**注:** Recharts v3 へのアップグレードに取り組んでいます。それまでの間、v3 のテストを開始したい場合は、[ここ](https://github.com/shadcn-ui/ui/issues/7669#issuecomment-2998299159) のコメント内のコードを参照してください。近々正式リリースを予定しております。

</Callout>

<ComponentPreview
  styleName="aria-nova"
  name="chart-demo"
  className="theme-blue [&_.preview]:h-auto [&_.preview]:p-0 [&_.preview]:lg:min-h-[404px] [&_.preview>div]:w-full [&_.preview>div]:border-none [&_.preview>div]:shadow-none"
  HideCode
/>

**チャート**を紹介します。コピーしてアプリに貼り付けることができるグラフ コンポーネントのコレクション。

チャートは、箱から出してすぐに見栄えがするように設計されています。これらは他のコンポーネントとうまく連携し、プロジェクトに合わせて完全にカスタマイズできます。

[チャート ライブラリを参照](/charts)。

## コンポーネント

内部では [Recharts](https://recharts.org/) を使用しています。

`chart` コンポーネントは、構成を念頭に置いて設計されました。 **Recharts コンポーネントを使用してグラフを作成し、`ChartTooltip` などのカスタム コンポーネントを必要なときに必要な場所にのみ取り込みます**。

```tsx showLineNumbers /ChartContainer/ /ChartTooltipContent/
import { Bar, BarChart } from "recharts"

import { ChartContainer, ChartTooltipContent } from "@/components/ui/chart"

export function MyChart() {
  return (
    <ChartContainer>
      <BarChart data={data}>
        <Bar dataKey="value" />
        <ChartTooltip content={<ChartTooltipContent />} />
      </BarChart>
    </ChartContainer>
  )
}
```

Recharts はラップしません。これは、抽象概念に囚われないことを意味します。 Recharts の新しいバージョンがリリースされたら、公式アップグレード パスに従ってチャートをアップグレードできます。

**コンポーネントはあなたのものです**。

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add chart
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install recharts
```

<Step>次のコードをコピーし、`components/ui/chart.tsx` に貼り付けます。</Step>

<ComponentSource
  name="chart"
  title="components/ui/chart.tsx"
  styleName="aria-nova"
/>

<Step>CSS ファイルに次の色を追加します</Step>

```css title="app/globals.css" showLineNumbers
@layer base {
  :root {
    --chart-1: oklch(0.646 0.222 41.116);
    --chart-2: oklch(0.6 0.118 184.704);
    --chart-3: oklch(0.398 0.07 227.392);
    --chart-4: oklch(0.828 0.189 84.429);
    --chart-5: oklch(0.769 0.188 70.08);
  }

  .dark {
    --chart-1: oklch(0.488 0.243 264.376);
    --chart-2: oklch(0.696 0.17 162.48);
    --chart-3: oklch(0.769 0.188 70.08);
    --chart-4: oklch(0.627 0.265 303.9);
    --chart-5: oklch(0.645 0.246 16.439);
  }
}
```

</Steps>

</TabsContent>

</CodeTabs>

## 初めてのチャート

最初のチャートを作成しましょう。バーチャートを作成し、グリッド、軸、ツールチップ、凡例を追加します。

<Steps>

<Step>データを定義することから始めます</Step>

次のデータは、各月のデスクトップ ユーザーとモバイル ユーザーの数を表します。

<Callout className="mt-4">

**注:** データは任意の形式にすることができます。以下のデータの形式に限定されません。 `dataKey` プロパティを使用して、データをチャートにマップします。

</Callout>

```tsx title="components/example-chart.tsx" showLineNumbers
const chartData = [
  { month: "January", desktop: 186, mobile: 80 },
  { month: "February", desktop: 305, mobile: 200 },
  { month: "March", desktop: 237, mobile: 120 },
  { month: "April", desktop: 73, mobile: 190 },
  { month: "May", desktop: 209, mobile: 130 },
  { month: "June", desktop: 214, mobile: 140 },
]
```

<Step>チャート構成を定義します</Step>

チャート設定はチャートの設定を保持します。ここに、ラベル、アイコン、テーマ用のカラートークンなど、人間が判読できる文字列を配置します。

```tsx title="components/example-chart.tsx" showLineNumbers
import { type ChartConfig } from "@/components/ui/chart"

const chartConfig = {
  desktop: {
    label: "Desktop",
    color: "#2563eb",
  },
  mobile: {
    label: "Mobile",
    color: "#60a5fa",
  },
} satisfies ChartConfig
```

<Step>チャートを作成します</Step>

これで、Recharts コンポーネントを使用してチャートを作成できるようになりました。

<Callout className="mt-4 bg-amber-50 border-amber-200 dark:bg-amber-950/50 dark:border-amber-950">

**重要:** `ChartContainer` コンポーネントに `min-h-[VALUE]` を設定することを忘れないでください。これはチャートの応答性を高めるために必要です。

</Callout>

<ComponentPreview
  styleName="aria-nova"
  name="chart-example"
  previewClassName="h-80"
/>

</Steps>

### グリッドの追加

Let's add a grid to the chart.

<Steps className="mb-0 pt-2">

<Step>`CartesianGrid` コンポーネントをインポートします。</Step>

```tsx /CartesianGrid/
import { Bar, BarChart, CartesianGrid } from "recharts"
```

<Step>`CartesianGrid` コンポーネントをチャートに追加します。</Step>

```tsx showLineNumbers {3}
<ChartContainer config={chartConfig} className="min-h-[200px] w-full">
  <BarChart accessibilityLayer data={chartData}>
    <CartesianGrid vertical={false} />
    <Bar dataKey="desktop" fill="var(--color-desktop)" radius={4} />
    <Bar dataKey="mobile" fill="var(--color-mobile)" radius={4} />
  </BarChart>
</ChartContainer>
```

<ComponentPreview
  styleName="aria-nova"
  name="chart-example-grid"
  previewClassName="h-80"
/>

</Steps>

### 軸の追加

グラフに X 軸を追加するには、`XAxis` コンポーネントを使用します。

<Steps className="mb-0 pt-2">

<Step>`XAxis` コンポーネントをインポートします。</Step>

```tsx /XAxis/
import { Bar, BarChart, CartesianGrid, XAxis } from "recharts"
```

<Step>`XAxis` コンポーネントをチャートに追加します。</Step>

```tsx showLineNumbers {4-10}
<ChartContainer config={chartConfig} className="h-[200px] w-full">
  <BarChart accessibilityLayer data={chartData}>
    <CartesianGrid vertical={false} />
    <XAxis
      dataKey="month"
      tickLine={false}
      tickMargin={10}
      axisLine={false}
      tickFormatter={(value) => value.slice(0, 3)}
    />
    <Bar dataKey="desktop" fill="var(--color-desktop)" radius={4} />
    <Bar dataKey="mobile" fill="var(--color-mobile)" radius={4} />
  </BarChart>
</ChartContainer>
```

<ComponentPreview
  styleName="aria-nova"
  name="chart-example-axis"
  previewClassName="h-80"
/>

</Steps>

### ツールチップの追加

これまで、Recharts のコンポーネントのみを使用してきました。 `chart` コンポーネントのカスタマイズのおかげで、箱から出してすぐに見栄えがよくなります。

ツールチップを追加するには、`chart` のカスタム `ChartTooltip` コンポーネントと `ChartTooltipContent` コンポーネントを使用します。

<Steps className="mb-0 pt-2">

<Step>`ChartTooltip` および `ChartTooltipContent` コンポーネントをインポートします。</Step>

```tsx
import { ChartTooltip, ChartTooltipContent } from "@/components/ui/chart"
```

<Step>コンポーネントをチャートに追加します。</Step>

```tsx showLineNumbers {11}
<ChartContainer config={chartConfig} className="h-[200px] w-full">
  <BarChart accessibilityLayer data={chartData}>
    <CartesianGrid vertical={false} />
    <XAxis
      dataKey="month"
      tickLine={false}
      tickMargin={10}
      axisLine={false}
      tickFormatter={(value) => value.slice(0, 3)}
    />
    <ChartTooltip content={<ChartTooltipContent />} />
    <Bar dataKey="desktop" fill="var(--color-desktop)" radius={4} />
    <Bar dataKey="mobile" fill="var(--color-mobile)" radius={4} />
  </BarChart>
</ChartContainer>
```

<ComponentPreview
  styleName="aria-nova"
  name="chart-example-tooltip"
  previewClassName="h-80"
/>

カーソルを合わせるとツールチップが表示されます。簡単ですよね？ 2 つのコンポーネントで、美しいツールチップが表示されます。

</Steps>

### 凡例の追加

凡例に対しても同じことを行います。 `chart` の `ChartLegend` コンポーネントと `ChartLegendContent` コンポーネントを使用します。

<Steps className="mb-0 pt-2">

<Step>`ChartLegend` および `ChartLegendContent` コンポーネントをインポートします。</Step>

```tsx
import { ChartLegend, ChartLegendContent } from "@/components/ui/chart"
```

<Step>コンポーネントをチャートに追加します。</Step>

```tsx showLineNumbers {12}
<ChartContainer config={chartConfig} className="h-[200px] w-full">
  <BarChart accessibilityLayer data={chartData}>
    <CartesianGrid vertical={false} />
    <XAxis
      dataKey="month"
      tickLine={false}
      tickMargin={10}
      axisLine={false}
      tickFormatter={(value) => value.slice(0, 3)}
    />
    <ChartTooltip content={<ChartTooltipContent />} />
    <ChartLegend content={<ChartLegendContent />} />
    <Bar dataKey="desktop" fill="var(--color-desktop)" radius={4} />
    <Bar dataKey="mobile" fill="var(--color-mobile)" radius={4} />
  </BarChart>
</ChartContainer>
```

<ComponentPreview
  styleName="aria-nova"
  name="chart-example-legend"
  previewClassName="h-80"
/>

</Steps>

完了しました。最初のチャートが作成されました。次は何でしょうか？

- [テーマと色](/docs/components/aria/chart#theming)
- [ツールチップ](/docs/components/aria/chart#tooltip)
- [凡例](/docs/components/aria/chart#legend)

## チャート設定

チャート設定では、チャートのラベル、アイコン、色を定義します。

チャート データから意図的に切り離されています。

これにより、チャート間で構成トークンとカラートークンを共有できるようになります。データまたはカラー トークンがリモートに存在する場合や、異なる形式で存在する場合でも、独立して機能することもできます。

```tsx showLineNumbers /ChartConfig/
import { Monitor } from "lucide-react"

import { type ChartConfig } from "@/components/ui/chart"

const chartConfig = {
  desktop: {
    label: "Desktop",
    icon: Monitor,
    // A color like 'hsl(220, 98%, 61%)' or 'var(--color-name)'
    color: "#2563eb",
    // OR a theme object with 'light' and 'dark' keys
    theme: {
      light: "#2563eb",
      dark: "#dc2626",
    },
  },
} satisfies ChartConfig
```

## テーマ

チャートにはテーマのサポートが組み込まれています。 CSS 変数 (推奨) または任意のカラー形式 (hex、hsl、oklch など) のカラー値を使用できます。

### CSS 変数

<Steps className="mb-0 pt-2">

<Step>CSS ファイルで色を定義します</Step>

```css title="app/globals.css" showLineNumbers
@layer base {
  :root {
    --chart-1: oklch(0.646 0.222 41.116);
    --chart-2: oklch(0.6 0.118 184.704);
  }

  .dark: {
    --chart-1: oklch(0.488 0.243 264.376);
    --chart-2: oklch(0.696 0.17 162.48);
  }
}
```

<Step>`chartConfig` に色を追加します</Step>

```tsx title="components/example-chart.tsx" showLineNumbers
const chartConfig = {
  desktop: {
    label: "Desktop",
    color: "var(--chart-1)",
  },
  mobile: {
    label: "Mobile",
    color: "var(--chart-2)",
  },
} satisfies ChartConfig
```

</Steps>

### hex、hsl、または oklch

チャート設定で直接色を定義することもできます。好みのカラー形式を使用してください。

```tsx title="components/example-chart.tsx" showLineNumbers
const chartConfig = {
  desktop: {
    label: "Desktop",
    color: "#2563eb",
  },
  mobile: {
    label: "Mobile",
    color: "hsl(220, 98%, 61%)",
  },
  tablet: {
    label: "Tablet",
    color: "oklch(0.5 0.2 240)",
  },
  laptop: {
    label: "Laptop",
    color: "var(--chart-2)",
  },
} satisfies ChartConfig
```

### 色の使用

チャートでテーマの色を使用するには、`var(--color-KEY)` の形式を使用して色を参照します。

#### コンポーネント

```tsx
<Bar dataKey="desktop" fill="var(--color-desktop)" />
```

#### チャート データ

```tsx title="components/example-chart.tsx" showLineNumbers
const chartData = [
  { browser: "chrome", visitors: 275, fill: "var(--color-chrome)" },
  { browser: "safari", visitors: 200, fill: "var(--color-safari)" },
]
```

#### Tailwind

```tsx title="components/example-chart.tsx"
<LabelList className="fill-[--color-desktop]" />
```

## ツールチップ

チャート ツールチップには、ラベル、名前、インジケーター、および値が含まれます。これらを組み合わせて使用して、ツールチップをカスタマイズできます。

<ComponentPreview styleName="aria-nova" name="chart-tooltip" hideCode />

`hideLabel`、`hideIndicator` プロパティを使用してこれらのオン/オフを切り替え、`indicator` プロパティを使用してインジケーターのスタイルをカスタマイズできます。

ツールチップのラベルと名前にカスタム キーを使用するには、`labelKey` および `nameKey` を使用します。

チャートには、`<ChartTooltip>` コンポーネントと `<ChartTooltipContent>` コンポーネントが付属しています。これら 2 つのコンポーネントを使用して、グラフにカスタム ツールチップを追加できます。

```tsx title="components/example-chart.tsx"
import { ChartTooltip, ChartTooltipContent } from "@/components/ui/chart"
```

```tsx title="components/example-chart.tsx"
<ChartTooltip content={<ChartTooltipContent />} />
```

### プロパティ

ツールチップをカスタマイズするには、次のプロパティを使用します。

|プロパティ |タイプ |説明 |
| :-------------- | :----------------------- | :------------------------------------------- |
| `labelKey` | 文字列 | ラベルに使用する構成キーまたはデータキー。 |
| `nameKey` | 文字列 | 名前に使用する構成キーまたはデータキー。 |
| `indicator` | `dot`、`line`、または `dashed` | ツールチップのインジケーターのスタイル。 |
| `hideLabel` | ブール値 | ラベルを非表示にするかどうか。 |
| `hideIndicator` | ブール値 | インジケーターを非表示にするかどうか。 |

### カラー

色はチャート設定から自動的に参照されます。

### カスタム

ツールチップのラベルと名前にカスタム キーを使用するには、`labelKey` および `nameKey` プロパティを使用します。

```tsx showLineNumbers /browser/
const chartData = [
  { browser: "chrome", visitors: 187, fill: "var(--color-chrome)" },
  { browser: "safari", visitors: 200, fill: "var(--color-safari)" },
]

const chartConfig = {
  visitors: {
    label: "Total Visitors",
  },
  chrome: {
    label: "Chrome",
    color: "var(--chart-1)",
  },
  safari: {
    label: "Safari",
    color: "var(--chart-2)",
  },
} satisfies ChartConfig
```

```tsx title="components/example-chart.tsx"
<ChartTooltip
  content={<ChartTooltipContent labelKey="visitors" nameKey="browser" />}
/>
```

これにより、ラベルに `Total Visitors` が使用され、ツールチップ名に `Chrome` と `Safari` が使用されます。

## 凡例

カスタム `<ChartLegend>` および `<ChartLegendContent>` コンポーネントを使用して、グラフに凡例を追加できます。

```tsx title="components/example-chart.tsx"
import { ChartLegend, ChartLegendContent } from "@/components/ui/chart"
```

```tsx title="components/example-chart.tsx"
<ChartLegend content={<ChartLegendContent />} />
```

### カラー

色はチャート設定から自動的に参照されます。

### カスタム

凡例名にカスタム キーを使用するには、`nameKey` プロパティを使用します。

```tsx showLineNumbers /browser/
const chartData = [
  { browser: "chrome", visitors: 187, fill: "var(--color-chrome)" },
  { browser: "safari", visitors: 200, fill: "var(--color-safari)" },
]

const chartConfig = {
  chrome: {
    label: "Chrome",
    color: "var(--chart-1)",
  },
  safari: {
    label: "Safari",
    color: "var(--chart-2)",
  },
} satisfies ChartConfig
```

```tsx title="components/example-chart.tsx"
<ChartLegend content={<ChartLegendContent nameKey="browser" />} />
```

これにより、凡例名に `Chrome` と `Safari` が使用されます。

## アクセシビリティ

`accessibilityLayer` プロパティをオンにして、アクセス可能なレイヤーをチャートに追加できます。

このプロパティは、チャートにキーボード アクセスとスクリーン リーダーのサポートを追加します。

```tsx title="components/example-chart.tsx"
<LineChart accessibilityLayer />
```

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="chart-rtl"
  direction="rtl"
  previewClassName="h-92"
/>
