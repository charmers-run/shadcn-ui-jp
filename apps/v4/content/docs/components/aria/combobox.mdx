---
title: コンボボックス
description: 候補のリストを含むオートコンプリート入力。
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/ComboBox
  api: https://react-aria.adobe.com/ComboBox#api
---

<ComponentPreview
  styleName="aria-nova"
  name="combobox-demo"
  description="フレームワークのリストが含まれるコンボボックス。"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add combobox
```

</TabsContent>
<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="combobox"
  title="components/ui/combobox.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import {
  Combobox,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
} from "@/components/ui/combobox"
```

```tsx showLineNumbers
const frameworks = ["Next.js", "SvelteKit", "Nuxt.js", "Remix", "Astro"]

export function ExampleCombobox() {
  return (
    <Combobox allowsEmptyCollection>
      <ComboboxInput placeholder="Select a framework" />
      <ComboboxContent>
        <ComboboxList
          renderEmptyState={() => (
            <ComboboxEmpty>No items found.</ComboboxEmpty>
          )}
        >
          {frameworks.map((item) => (
            <ComboboxItem key={item} id={item}>
              {item}
            </ComboboxItem>
          ))}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  )
}
```

## 構成

### シンプル

単一行入力とフラット リスト ([基本](#basic) を参照)。

```text
Combobox
├── ComboboxInput
└── ComboboxContent
    └── ComboboxList
        ├── ComboboxItem
        └── ComboboxItem
```

### チップ付き

`multiple`、チップ、およびチップ入力による複数選択 ([複数](#multiple) を参照)。

```text
Combobox
├── ComboboxChips
│   ├── ComboboxChipList
│   │   └── ComboboxChip
│   └── ComboboxChipsInput
└── ComboboxContent
    └── ComboboxList
        ├── ComboboxItem
        └── ComboboxItem
```

### グループとコレクションを使用する

`ComboboxGroup` を使用してグループごとに項目をネストし、グループ間に区切り文字を付けます ([グループ](#groups) を参照)。

```text
Combobox
├── ComboboxInput
└── ComboboxContent
    └── ComboboxList
        ├── ComboboxGroup
        │   ├── ComboboxLabel
        │   ├── ComboboxItem
        │   └── ComboboxItem
        ├── ComboboxSeparator
        └── ComboboxGroup
            ├── ComboboxLabel
            ├── ComboboxItem
            └── ComboboxItem
```

## カスタム項目

アイテムが JSX の子をレンダリングする場合は `textValue` を使用します。

```tsx showLineNumbers
import * as React from "react"

import {
  Combobox,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
} from "@/components/ui/combobox"

type Framework = {
  label: string
  value: string
}

const frameworks: Framework[] = [
  { label: "Next.js", value: "next" },
  { label: "SvelteKit", value: "sveltekit" },
  { label: "Nuxt", value: "nuxt" },
]

export function ExampleComboboxCustomItems() {
  return (
    <Combobox allowsEmptyCollection>
      <ComboboxInput placeholder="Select a framework" />
      <ComboboxContent>
        <ComboboxList
          renderEmptyState={() => (
            <ComboboxEmpty>No items found.</ComboboxEmpty>
          )}
          items={frameworks}
        >
          {(framework) => (
            <ComboboxItem id={framework.value} textValue={framework.label}>
              <span>{framework.label}</span>
            </ComboboxItem>
          )}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  )
}
```

## 複数選択

複数選択動作のチップでは `multiple` を使用します。

```tsx showLineNumbers
import * as React from "react"

import {
  Combobox,
  ComboboxChip,
  ComboboxChipList,
  ComboboxChips,
  ComboboxChipsInput,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
} from "@/components/ui/combobox"

const frameworks = ["Next.js", "SvelteKit", "Nuxt.js", "Remix", "Astro"]

export function ExampleComboboxMultiple() {
  const [value, setValue] = React.useState<string[]>([])

  return (
    <Combobox
      selectionMode="multiple"
      value={value}
      onChange={setValue}
      allowsEmptyCollection
    >
      <ComboboxChips>
        <ComboboxChipList<{ name: string }>>
          {(value) => <ComboboxChip id={value.name}>{value.name}</ComboboxChip>}
        </ComboboxChipList>
        <ComboboxChipsInput placeholder="Add framework" />
      </ComboboxChips>
      <ComboboxContent>
        <ComboboxList
          renderEmptyState={() => (
            <ComboboxEmpty>No items found.</ComboboxEmpty>
          )}
        >
          {frameworks.map((item) => (
            <ComboboxItem key={item} id={item}>
              {item}
            </ComboboxItem>
          ))}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  )
}
```

## 基本

フレームワークのリストを含む単純なコンボボックス。

<ComponentPreview styleName="aria-nova" name="combobox-basic" />

## 複数

`multiple` と `ComboboxChips` を使用した複数選択のコンボボックス。

<ComponentPreview styleName="aria-nova" name="combobox-multiple" />

## クリア ボタン

`showClear` プロップを使用してクリア ボタンを表示します。

<ComponentPreview styleName="aria-nova" name="combobox-clear" />

## グループ

`ComboboxGroup` および `ComboboxSeparator` を使用して項目をグループ化します。

<ComponentPreview styleName="aria-nova" name="combobox-groups" />

## カスタム項目

`ComboboxItem` 内でカスタム コンポーネントをレンダリングできます。

<ComponentPreview styleName="aria-nova" name="combobox-custom" />

## 無効な状態

コンボボックスを無効な状態にするには、`isInvalid` プロパティを使用します。

<ComponentPreview styleName="aria-nova" name="combobox-invalid" />

## 無効

`isDisabled` プロパティを使用してコンボボックスを無効にします。

<ComponentPreview styleName="aria-nova" name="combobox-disabled" />

## 入力グループ

`ComboboxInput` 内の `InputGroupAddon` コンポーネントを使用して、コンボボックスにアドオンを追加できます。

<ComponentPreview styleName="aria-nova" name="combobox-input-group" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="combobox-rtl"
  direction="rtl"
  align="start"
/>

## API リファレンス

詳細については、[React Aria](https://react-aria.adobe.com/ComboBox#api) ドキュメントを参照してください。
