---
title: アコーディオン
description: 垂直方向に積み重ねられたインタラクティブな見出しのセットで、それぞれがコンテンツのセクションを表示します。
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/DisclosureGroup
  api: https://react-aria.adobe.com/DisclosureGroup#api
---

<ComponentPreview
  styleName="aria-nova"
  name="accordion-demo"
  align="start"
  previewClassName="*:data-[slot=accordion]:max-w-sm h-[300px]"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add accordion
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="accordion"
  title="components/ui/accordion.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@/components/ui/accordion"
```

```tsx showLineNumbers
<Accordion defaultExpandedKeys={["item-1"]}>
  <AccordionItem id="item-1">
    <AccordionTrigger>Is it accessible?</AccordionTrigger>
    <AccordionContent>
      Yes. It adheres to the WAI-ARIA design pattern.
    </AccordionContent>
  </AccordionItem>
</Accordion>
```

## 構成

次の構成で `Accordion` を作成します。

```text
Accordion
├── AccordionItem
│   ├── AccordionTrigger
│   └── AccordionContent
└── AccordionItem
    ├── AccordionTrigger
    └── AccordionContent
```

## 基本

一度に 1 つの項目を表示する基本的なアコーディオンです。デフォルトでは最初の項目が開きます。

<ComponentPreview
  styleName="aria-nova"
  name="accordion-basic"
  align="start"
  previewClassName="*:data-[slot=accordion]:max-w-sm h-[300px]"
/>

## 複数

`allowsMultipleExpanded` プロパティを使うと、複数の項目を同時に開けます。

<ComponentPreview
  styleName="aria-nova"
  name="accordion-multiple"
  align="start"
  previewClassName="*:data-[slot=accordion]:max-w-sm h-[450px]"
/>

## 無効

`AccordionItem` の `isDisabled` プロパティを使うと、個別の項目を無効にできます。

<ComponentPreview
  styleName="aria-nova"
  name="accordion-disabled"
  align="start"
  previewClassName="*:data-[slot=accordion]:max-w-sm h-[300px]"
/>

## 境界線

項目に境界線を追加するには、`Accordion` に `border` を、`AccordionItem` に `border-b last:border-b-0` を追加します。

<ComponentPreview
  styleName="aria-nova"
  name="accordion-borders"
  align="start"
  previewClassName="*:data-[slot=accordion]:max-w-sm h-[300px]"
/>

## カード

`Accordion` を `Card` コンポーネントで囲みます。

<ComponentPreview
  styleName="aria-nova"
  name="accordion-card"
  align="start"
  previewClassName="*:data-[slot=accordion]:max-w-sm h-[435px]"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="accordion-rtl"
  align="start"
  direction="rtl"
/>

## API リファレンス

[React Aria](https://react-aria.adobe.com/DisclosureGroup#api) のドキュメントで詳細を確認できます。
