---
title: コンテキスト メニュー
description: 右クリックによってトリガーされるアクションのメニューを表示します。
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/Menu
  api: https://react-aria.adobe.com/Menu#api
---

<ComponentPreview
  styleName="aria-nova"
  name="context-menu-demo"
  description="サブメニュー項目を含むコンテキスト メニュー。"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add context-menu
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="context-menu"
  title="components/ui/context-menu.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import { Pressable } from "react-aria-components"

import {
  ContextMenu,
  ContextMenuItem,
  ContextMenuTrigger,
} from "@/components/ui/context-menu"
```

```tsx showLineNumbers
<ContextMenuTrigger>
  <Pressable>
    <div role="button">Right click here</div>
  </Pressable>
  <ContextMenu>
    <ContextMenuItem>Profile</ContextMenuItem>
    <ContextMenuItem>Billing</ContextMenuItem>
    <ContextMenuItem>Team</ContextMenuItem>
    <ContextMenuItem>Subscription</ContextMenuItem>
  </ContextMenu>
</ContextMenuTrigger>
```

## 構成

次の構成を使用して `ContextMenu` を作成します。

```text
ContextMenuTrigger
├── Pressable
└── ContextMenuContent
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   ├── ContextMenuItem
    │   └── ContextMenuItem
    ├── ContextMenuSeparator
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   ├── ContextMenuItem
    │   └── ContextMenuItem
    └── ContextMenuSub
        ├── ContextMenuSubTrigger
        └── ContextMenuSubContent
            └── ContextMenuGroup
                ├── ContextMenuItem
                └── ContextMenuItem
```

## 基本

いくつかのアクションを備えたシンプルなコンテキスト メニュー。

<ComponentPreview styleName="aria-nova" name="context-menu-basic" />

## サブメニュー

二次アクションをネストするには `ContextMenuSub` を使用します。

<ComponentPreview styleName="aria-nova" name="context-menu-submenu" />

## ショートカット

キーボード ヒントを表示するには、`ContextMenuShortcut` を追加します。

<ComponentPreview styleName="aria-nova" name="context-menu-shortcuts" />

## グループ

関連するアクションをグループ化し、区切り線で区切ります。

<ComponentPreview styleName="aria-nova" name="context-menu-groups" />

## アイコン

アイコンとラベルを組み合わせて、すばやくスキャンします。

<ComponentPreview styleName="aria-nova" name="context-menu-icons" />

## チェックボックス

切り替えには `selectionMode="multiple"` を使用します。

<ComponentPreview styleName="aria-nova" name="context-menu-checkboxes" />

## ラジオ

排他的な選択には `selectionMode="single"` を使用します。

<ComponentPreview styleName="aria-nova" name="context-menu-radio" />

## 破壊的

`variant="destructive"` を使用して、メニュー項目を破壊的なスタイルに設定します。

<ComponentPreview styleName="aria-nova" name="context-menu-destructive" />

## 配置

`placement` プロパティを使用してサブメニューの配置を制御します。

<ComponentPreview styleName="aria-nova" name="context-menu-sides" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="context-menu-rtl"
  direction="rtl"
/>

`placement="end"` を使用して、トリガーの論理終了側にメニューを配置します。

```tsx showLineNumbers
<ContextMenuTrigger>
  <Pressable>
    <div role="button">Right click here</div>
  </Pressable>
  <ContextMenu placement="end">
    <ContextMenuItem>Profile</ContextMenuItem>
    <ContextMenuItem>Billing</ContextMenuItem>
    <ContextMenuItem>Team</ContextMenuItem>
    <ContextMenuItem>Subscription</ContextMenuItem>
  </ContextMenu>
</ContextMenuTrigger>
```

## API リファレンス

詳細については、[React Aria](https://react-aria.adobe.com/Menu#api) ドキュメントを参照してください。
