---
title: 入力
description: スタイル機能とアクセシビリティ機能が組み込まれたフォームおよびユーザー データ入力用のテキスト入力コンポーネント。
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/TextField
  api: https://react-aria.adobe.com/TextField#api
---

<ComponentPreview
  styleName="aria-nova"
  name="input-demo"
  previewClassName="*:max-w-xs"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add input
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="input"
  title="components/ui/input.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx
import { Input } from "@/components/ui/input"
```

```tsx
<Input />
```

## 基本

<ComponentPreview
  styleName="aria-nova"
  name="input-basic"
  previewClassName="*:max-w-xs"
/>

## フィールド

`Field`、`FieldLabel`、および `FieldDescription` を使用して、ラベルと説明を追加します。

<ComponentPreview
  styleName="aria-nova"
  name="input-field"
  previewClassName="*:max-w-xs"
/>

## フィールド グループ

`FieldGroup` を使用して、複数の `Field` ブロックを表示し、フォームを構築します。

<ComponentPreview
  styleName="aria-nova"
  name="input-fieldgroup"
  previewClassName="*:max-w-xs"
/>

## 無効

入力を無効にするには、`disabled` プロパティを使用します。無効な状態のスタイルを設定するには、`data-disabled` 属性を `Field` コンポーネントに追加します。

<ComponentPreview
  styleName="aria-nova"
  name="input-disabled"
  previewClassName="*:max-w-xs"
/>

## 無効な入力

`aria-invalid` プロパティを使用して、入力を無効としてマークします。無効な状態のスタイルを設定するには、`data-invalid` 属性を `Field` コンポーネントに追加します。

<ComponentPreview
  styleName="aria-nova"
  name="input-invalid"
  previewClassName="*:max-w-xs"
/>

## ファイル

`type="file"` プロパティを使用して、ファイル入力を作成します。

<ComponentPreview
  styleName="aria-nova"
  name="input-file"
  previewClassName="*:max-w-xs"
/>

## インライン

インライン入力を作成するには、`Field` を `orientation="horizontal"` とともに使用します。`Button` と組み合わせて、ボタン付きの検索入力を作成します。

<ComponentPreview
  styleName="aria-nova"
  name="input-inline"
  previewClassName="*:max-w-xs"
/>

## グリッド

グリッド レイアウトを使用して、複数の入力を並べて配置します。

<ComponentPreview
  styleName="aria-nova"
  name="input-grid"
  previewClassName="p-6"
/>

## 必須

`required` 属性を使用して、必須の入力を示します。

<ComponentPreview
  styleName="aria-nova"
  name="input-required"
  previewClassName="*:max-w-xs"
/>

## バッジ

推奨フィールドを強調表示するには、ラベルで `Badge` を使用します。

<ComponentPreview
  styleName="aria-nova"
  name="input-badge"
  previewClassName="*:max-w-xs"
/>

## 入力グループ

入力内にアイコン、テキスト、またはボタンを追加するには、`InputGroup` コンポーネントを使用します。その他の例については、[入力グループ](/docs/components/aria/input-group) コンポーネントを参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="input-input-group"
  previewClassName="*:max-w-xs"
/>

## ボタン グループ

入力にボタンを追加するには、`ButtonGroup` コンポーネントを使用します。その他の例については、[ボタン グループ](/docs/components/aria/button-group) コンポーネントを参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="input-button-group"
  previewClassName="*:max-w-xs"
/>

## フォーム

複数の入力、選択、ボタンを含む完全なフォームの例。

<ComponentPreview
  styleName="aria-nova"
  name="input-form"
  previewClassName="h-[32rem]"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="input-rtl"
  direction="rtl"
  previewClassName="*:max-w-xs"
/>
