---
title: 入力グループ
description: アドオン、ボタン、ヘルパー コンテンツを入力に追加します。
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/Group
  api: https://react-aria.adobe.com/Group#api
---

import { IconInfoCircle } from "@tabler/icons-react"

<ComponentPreview
  styleName="aria-nova"
  name="input-group-demo"
  previewClassName="h-[26rem]"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add input-group
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="input-group"
  title="components/ui/input-group.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import {
  InputGroup,
  InputGroupAddon,
  InputGroupButton,
  InputGroupInput,
  InputGroupText,
  InputGroupTextarea,
} from "@/components/ui/input-group"
```

```tsx showLineNumbers
<InputGroup>
  <InputGroupInput placeholder="Search..." />
  <InputGroupAddon>
    <SearchIcon />
  </InputGroupAddon>
</InputGroup>
```

## 構成

次の構成を使用して `InputGroup` を構築します。

```text
InputGroup
├── InputGroupInput or InputGroupTextarea
├── InputGroupAddon
├── InputGroupButton
└── InputGroupText
```

## 整列

`InputGroupAddon` の `align` プロパティを使用して、入力を基準にしてアドオンを配置します。

<Callout>
  適切なフォーカス管理のため、`InputGroupAddon` は常に `InputGroupInput` または
  `InputGroupTextarea` の後に配置してください。アドオンの視覚的な配置には `align`
  プロパティを使用します。
</Callout>

### インラインスタート

`align="inline-start"` を使用して、アドオンを入力の先頭に配置します。これがデフォルトです。

<ComponentPreview
  styleName="aria-nova"
  name="input-group-inline-start"
  previewClassName="h-48"
/>

### インラインエンド

`align="inline-end"` を使用してアドオンを入力の最後に配置します。

<ComponentPreview
  styleName="aria-nova"
  name="input-group-inline-end"
  previewClassName="h-48"
/>

### ブロックスタート

`align="block-start"` を使用してアドオンを入力の上に配置します。

<ComponentPreview
  styleName="aria-nova"
  name="input-group-block-start"
  previewClassName="h-96"
/>

### ブロックエンド

`align="block-end"` を使用してアドオンを入力の下に配置します。

<ComponentPreview
  styleName="aria-nova"
  name="input-group-block-end"
  previewClassName="h-[26rem]"
/>

## アイコン

<ComponentPreview
  styleName="aria-nova"
  name="input-group-icon"
  previewClassName="h-80"
/>

## テキスト

<ComponentPreview
  styleName="aria-nova"
  name="input-group-text"
  previewClassName="h-80"
/>

## ボタン

<ComponentPreview
  styleName="aria-nova"
  name="input-group-button"
  previewClassName="h-72"
/>

## Kbd

<ComponentPreview
  styleName="aria-nova"
  name="input-group-kbd"
  previewClassName="h-40"
/>

## ドロップダウン

<ComponentPreview
  styleName="aria-nova"
  name="input-group-dropdown"
  previewClassName="h-56"
/>

## スピナー

<ComponentPreview
  styleName="aria-nova"
  name="input-group-spinner"
  previewClassName="h-80"
/>

## テキストエリア

<ComponentPreview
  styleName="aria-nova"
  name="input-group-textarea"
  previewClassName="h-96"
/>

## カスタム入力

自動フォーカス状態処理のために、`data-slot="input-group-control"` 属性をカスタム入力に追加します。

以下は、サードパーティ ライブラリからのカスタムのサイズ変更可能なテキストエリアの例です。

<ComponentPreview
  styleName="aria-nova"
  name="input-group-custom"
  previewClassName="h-56"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="input-group-rtl"
  direction="rtl"
  previewClassName="h-[30rem]"
/>

## API リファレンス

### 入力グループ

入力とアドオンをラップするメインコンポーネント。

|プロパティ |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<InputGroup>
  <InputGroupInput />
  <InputGroupAddon />
</InputGroup>
```

### InputGroupAddon

アイコン、テキスト、ボタン、またはその他のコンテンツを入力と一緒に表示します。

<Callout icon={<IconInfoCircle />} title="フォーカス ナビゲーション">
  適切なフォーカスナビゲーションのため、`InputGroupAddon` コンポーネントは
  `InputGroupInput` または `InputGroupTextarea` コンポーネントの後に配置してください。
  アドオンの配置には `align` プロパティを設定します。
</Callout>

|プロパティ |タイプ |デフォルト |
| ----------- | ---------------------------------------------------------------- | ---------------- |
| `align` | `"inline-start" \| "inline-end" \| "block-start" \| "block-end"` | `"inline-start"` |
| `className` | `string` |                  |

```tsx
<InputGroupAddon align="inline-end">
  <SearchIcon />
</InputGroupAddon>
```

**`<InputGroupInput />` の場合は、`inline-start` または `inline-end` の位置合わせを使用します。 `<InputGroupTextarea />` の場合は、`block-start` または `block-end` 配置を使用します。**

`InputGroupAddon` コンポーネントには、複数の `InputGroupButton` コンポーネントとアイコンを含めることができます。

```tsx
<InputGroupAddon>
  <InputGroupButton>Button</InputGroupButton>
  <InputGroupButton>Button</InputGroupButton>
</InputGroupAddon>
```

### 入力グループボタン

入力グループ内のボタンを表示します。

|プロパティ |タイプ |デフォルト |
| ----------- | ----------------------------------------------------------------------------- | --------- |
| `size` | `"xs" \| "icon-xs" \| "sm" \| "icon-sm"` | `"xs"` |
| `variant` | `"default" \| "destructive" \| "outline" \| "secondary" \| "ghost" \| "link"` | `"ghost"` |
| `className` | `string` |           |

```tsx
<InputGroupButton>Button</InputGroupButton>
<InputGroupButton size="icon-xs" aria-label="Copy">
  <CopyIcon />
</InputGroupButton>
```

### InputGroupInput

入力グループを構築するときの `<Input />` の置き換え。このコンポーネントには入力グループ スタイルが事前に適用されており、フォーカス状態の処理に統合された `data-slot="input-group-control"` を使用します。

|プロパティ |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` |         |

他のすべてのプロパティは、基礎となる `<Input />` コンポーネントに渡されます。

```tsx
<InputGroup>
  <InputGroupInput placeholder="Enter text..." />
  <InputGroupAddon>
    <SearchIcon />
  </InputGroupAddon>
</InputGroup>
```

### InputGroupTextarea

入力グループを構築するときの `<Textarea />` の置き換え。このコンポーネントにはテキストエリア グループ スタイルが事前に適用されており、フォーカス状態の処理に統一された `data-slot="input-group-control"` を使用します。

|プロパティ |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` |         |

他のすべてのプロパティは、基礎となる `<Textarea />` コンポーネントに渡されます。

```tsx
<InputGroup>
  <InputGroupTextarea placeholder="Enter message..." />
  <InputGroupAddon align="block-end">
    <InputGroupButton>Send</InputGroupButton>
  </InputGroupAddon>
</InputGroup>
```
