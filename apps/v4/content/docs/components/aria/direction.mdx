---
title: 方向
description: アプリケーションのテキスト方向を設定するプロバイダー コンポーネント。
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/I18nProvider
  api: https://react-aria.adobe.com/I18nProvider#api
---

`DirectionProvider` コンポーネントは、アプリケーションのテキスト方向 (`ltr` または `rtl`) を設定するために使用されます。これは、アラビア語、ヘブライ語、ペルシア語などの右から左に記述する言語をサポートするために不可欠です。

RTL モードでのコンポーネントのプレビューを次に示します。言語を切り替えるには、言語セレクターを使用します。さらに例を参照するには、コンポーネント ページの RTL セクションを探してください。

<ComponentPreview
  styleName="aria-nova"
  name="card-rtl"
  direction="rtl"
  previewClassName="h-auto"
  hideCode
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add direction
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="direction"
  title="components/ui/direction.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import { DirectionProvider } from "@/components/ui/direction"
```

```tsx showLineNumbers
<html dir="rtl">
  <body>
    <DirectionProvider direction="rtl">
      {/* Your app content */}
    </DirectionProvider>
  </body>
</html>
```

## useDirection

`useDirection` フックは、アプリケーションの現在の方向を取得するために使用されます。

```tsx showLineNumbers
import { useDirection } from "@/components/ui/direction"

function MyComponent() {
  const direction = useDirection()
  return <div>Current direction: {direction}</div>
}
```
