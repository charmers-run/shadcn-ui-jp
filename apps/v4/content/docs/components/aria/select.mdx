---
title: セレクト
description: ボタンで開き、ユーザーが選択できるオプションのリストを表示します。
base: aria
component: true
featured: true
links:
  doc: https://react-aria.adobe.com/Select
  api: https://react-aria.adobe.com/Select#api
---

<ComponentPreview styleName="aria-nova" name="select-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add select
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="select"
  title="components/ui/select.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  Select,
  SelectContent,
  SelectGroup,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select"
```

```tsx showLineNumbers
const items = [
  { label: "Light", value: "light" },
  { label: "Dark", value: "dark" },
  { label: "System", value: "system" },
]

<Select placeholder="Theme">
  <SelectTrigger className="w-[180px]">
    <SelectValue />
  </SelectTrigger>
  <SelectContent>
    <SelectGroup>
      {items.map((item) => (
        <SelectItem key={item.value} id={item.value}>
          {item.label}
        </SelectItem>
      ))}
    </SelectGroup>
  </SelectContent>
</Select>
```

## 構成

次の構成を使用して、`Select` を構築します。

```text
Select
├── SelectTrigger
│   └── SelectValue
└── SelectContent
    ├── SelectGroup
    │   ├── SelectLabel
    │   ├── SelectItem
    │   └── SelectItem
    ├── SelectSeparator
    └── SelectGroup
        ├── SelectLabel
        ├── SelectItem
        └── SelectItem
```

## グループ

`SelectGroup`、`SelectLabel`、`SelectSeparator` を使用して項目をグループ化します。

<ComponentPreview styleName="aria-nova" name="select-groups" />

## スクロール可能

多くの項目を含み、スクロール可能なセレクト。

<ComponentPreview styleName="aria-nova" name="select-scrollable" />

## 無効

<ComponentPreview styleName="aria-nova" name="select-disabled" />

## 無効な状態

エラー状態を表示するには、`Field` コンポーネントに `data-invalid` 属性を追加し、`SelectTrigger` コンポーネントに `isInvalid` プロパティを追加します。

```tsx showLineNumbers /data-invalid/ /aria-invalid/
<Field data-invalid>
  <FieldLabel>Fruit</FieldLabel>
  <Select isInvalid>{/* ... */}</Select>
</Field>
```

<ComponentPreview styleName="aria-nova" name="select-invalid" />

## 検索可能

React Aria の [Autocomplete](https://react-aria.adobe.com/Autocomplete) コンポーネントを使用して、検索サポートを追加します。

<ComponentPreview styleName="aria-nova" name="select-autocomplete" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview styleName="aria-nova" name="select-rtl" direction="rtl" />

## API リファレンス

詳細については、[React Aria Select](https://react-aria.adobe.com/Select#api) のドキュメントを参照してください。
