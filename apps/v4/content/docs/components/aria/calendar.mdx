---
title: カレンダー
description: 日付または日付範囲を選択できるカレンダーコンポーネントです。
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/Calendar
  api: https://react-aria.adobe.com/Calendar#api
---

<ComponentPreview
  styleName="aria-nova"
  name="calendar-demo"
  previewClassName="h-96"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add calendar
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components @internationalized/date
```

<Step>`Button` コンポーネントをプロジェクトに追加します。</Step>

`Calendar` コンポーネントは `Button` コンポーネントを使用します。プロジェクトにインストールされていることを確認してください。

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="calendar"
  title="components/ui/calendar.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import {
  getLocalTimeZone,
  today,
  type CalendarDate,
} from "@internationalized/date"

import { Calendar } from "@/components/ui/calendar"
```

```tsx showLineNumbers
const [date, setDate] = React.useState<CalendarDate | null>(
  today(getLocalTimeZone())
)

return (
  <Calendar value={date} onChange={setDate} className="rounded-lg border" />
)
```

詳細については、[React Aria](https://react-aria.adobe.com/Calendar) ドキュメントを参照してください。

## 日付ピッカー

`<Calendar>` コンポーネントを使用して日付ピッカーを構築できます。詳細については、[日付ピッカー](/docs/components/aria/date-picker) ページを参照してください。

## 国際カレンダー

ペルシャ、イスラム、仏教などの国際暦システムを使用するには、React Aria の `I18nProvider` を使用してロケールを設定します。

<ComponentPreview
  styleName="aria-nova"
  name="calendar-hijri"
  title="Persian / Hijri / Jalali Calendar"
  description="ペルシャのカレンダーです。"
  previewClassName="h-[400px]"
/>

## 基本

基本的なカレンダー コンポーネント。カレンダーのスタイルには `className="rounded-lg border"` を使用しました。

<ComponentPreview
  styleName="aria-nova"
  name="calendar-basic"
  previewClassName="h-96"
/>

## 範囲カレンダー

`RangeCalendar` コンポーネントを使用して範囲選択を有効にします。

<ComponentPreview
  styleName="aria-nova"
  name="calendar-range"
  previewClassName="h-[36rem] md:h-96"
/>

## 月と年のセレクター

月と年のドロップダウンを表示するには、`captionLayout="dropdown"` を使用します。

<ComponentPreview
  styleName="aria-nova"
  name="calendar-caption"
  previewClassName="h-96"
/>

## プリセット

<ComponentPreview
  styleName="aria-nova"
  name="calendar-presets"
  previewClassName="h-[650px]"
/>

## 日付と時刻のピッカー

<ComponentPreview
  styleName="aria-nova"
  name="calendar-time"
  previewClassName="h-[600px]"
/>

## 予約済みの日付

<ComponentPreview
  styleName="aria-nova"
  name="calendar-booked-dates"
  previewClassName="h-96"
/>

## カスタムセルサイズ

<ComponentPreview
  styleName="aria-nova"
  name="calendar-custom-days"
  title="カスタムセルサイズ"
  description="レスポンシブなカスタムセルサイズのカレンダー。"
  className="**:[.preview]:h-[560px]"
/>

`--cell-size` CSS 変数を使用して、カレンダーのセルのサイズをカスタマイズできます。ブレークポイント固有の値を使用して応答性を高めることもできます。

```tsx showLineNumbers
<Calendar
  value={date}
  onChange={setDate}
  className="rounded-lg border [--cell-size:--spacing(11)] md:[--cell-size:--spacing(12)]"
/>
```

または、固定値を使用します。

```tsx showLineNumbers
<Calendar
  value={date}
  onChange={setDate}
  className="rounded-lg border [--cell-size:2.75rem] md:[--cell-size:3rem]"
/>
```

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

ペルシア語/ヒジュラ語/ジャラリ語などの国際カレンダーを有効にする方法については、[国際カレンダー ガイド](#international-calendars)も参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="calendar-rtl"
  direction="rtl"
  previewClassName="h-96"
/>

## API リファレンス

`Calendar` コンポーネントの詳細については、[React Aria](https://react-aria.adobe.com/Calendar) ドキュメントを参照してください。
