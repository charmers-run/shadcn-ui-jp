---
title: シート
description: Dialog コンポーネントを拡張し、画面のメインコンテンツを補完するコンテンツを表示します。
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/Modal
  api: https://react-aria.adobe.com/Modal#api
---

<ComponentPreview styleName="aria-nova" name="sheet-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add sheet
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="sheet"
  title="components/ui/sheet.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  Sheet,
  SheetClose,
  SheetDescription,
  SheetFooter,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@/components/ui/sheet"
```

```tsx showLineNumbers
<SheetTrigger>
  <Button>Open</Button>
  <Sheet>
    <SheetHeader>
      <SheetTitle>Are you absolutely sure?</SheetTitle>
      <SheetDescription>This action cannot be undone.</SheetDescription>
    </SheetHeader>
  </Sheet>
</SheetTrigger>
```

## 構成

以下の構成で `Sheet` を作成します。

```text
SheetTrigger
├── Button
└── Sheet
    ├── SheetHeader
    │   ├── SheetTitle
    │   └── SheetDescription
    └── SheetFooter
```

## サイド

`SheetContent` の `side` プロップを使用して、シートが表示される画面の端を指定します。値は `top`、`right`、`bottom`、`left` のいずれかです。

<ComponentPreview styleName="aria-nova" name="sheet-side" />

## 閉じるボタンなし

`SheetContent` で `showCloseButton={false}` を使用すると、閉じるボタンを非表示にできます。

<ComponentPreview styleName="aria-nova" name="sheet-no-close-button" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview styleName="aria-nova" name="sheet-rtl" direction="rtl" />

## API リファレンス

[React Aria Modal](https://react-aria.adobe.com/Modal#api) のドキュメントを参照してください。
