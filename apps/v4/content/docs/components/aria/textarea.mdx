---
title: テキストエリア
description: フォームのテキストエリア、またはテキストエリアに似たコンポーネントを表示します。
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/TextField
  api: https://react-aria.adobe.com/TextField#api
---

<ComponentPreview
  styleName="aria-nova"
  name="textarea-demo"
  previewClassName="*:max-w-xs"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">マニュアル</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add textarea
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="textarea"
  title="components/ui/textarea.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx
import { Textarea } from "@/components/ui/textarea"
```

```tsx
<Textarea />
```

## フィールド

`Field`、`FieldLabel`、`FieldDescription` を使用して、ラベルと説明を含むテキストエリアを作成します。

<ComponentPreview
  styleName="aria-nova"
  name="textarea-field"
  previewClassName="*:max-w-xs"
/>

## 無効

`disabled` プロパティを使用してテキストエリアを無効にします。無効な状態のスタイルを設定するには、`data-disabled` 属性を `Field` コンポーネントに追加します。

<ComponentPreview
  styleName="aria-nova"
  name="textarea-disabled"
  previewClassName="*:max-w-xs"
/>

## 無効な状態

`aria-invalid` プロパティを使用して、テキストエリアを無効としてマークします。無効な状態のスタイルを設定するには、`data-invalid` 属性を `Field` コンポーネントに追加します。

<ComponentPreview
  styleName="aria-nova"
  name="textarea-invalid"
  previewClassName="*:max-w-xs"
/>

## ボタン

`Button` と組み合わせて、送信ボタンのあるテキストエリアを作成します。

<ComponentPreview
  styleName="aria-nova"
  name="textarea-button"
  previewClassName="*:max-w-xs"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview styleName="aria-nova" name="textarea-rtl" direction="rtl" />
