---
title: テーブル
description: レスポンシブなテーブル コンポーネント。
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/Table
  api: https://react-aria.adobe.com/Table#api
---

<ComponentPreview
  styleName="aria-nova"
  name="table-demo"
  previewClassName="h-[30rem]"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add table
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="table"
  title="components/ui/table.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  Table,
  TableBody,
  TableCaption,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table"
```

```tsx showLineNumbers
<Table>
  <TableHeader>
    <TableHead isRowHeader className="w-[100px]">Invoice</TableHead>
    <TableHead>Status</TableHead>
    <TableHead>Method</TableHead>
    <TableHead className="text-right">Amount</TableHead>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell className="font-medium">INV001</TableCell>
      <TableCell>Paid</TableCell>
      <TableCell>Credit Card</TableCell>
      <TableCell className="text-right">$250.00</TableCell>
    </TableRow>
  </TableBody>
</Table>
<TableCaption>A list of your recent invoices.</TableCaption>
```

## 構成

次の構成を使用して、`Table` を構築します。

```text
Table
├── TableCaption
├── TableHeader
│   └── TableRow
│       ├── TableHead
│       ├── TableHead
│       ├── TableHead
│       └── TableHead
├── TableBody
│   ├── TableRow
│   │   ├── TableCell
│   │   ├── TableCell
│   │   ├── TableCell
│   │   └── TableCell
│   └── TableRow
│       ├── TableCell
│       ├── TableCell
│       ├── TableCell
│       └── TableCell
└── TableFooter
```

## フッター

`<TableFooter />` コンポーネントを使用して、テーブルにフッターを追加します。

<ComponentPreview styleName="aria-nova" name="table-footer" />

## アクション

`<DropdownMenu />` コンポーネントを使用した各行のアクションを示すテーブル。

<ComponentPreview styleName="aria-nova" name="table-actions" />

## データ テーブル

`<Table />` コンポーネントを使用して、より複雑なデータ テーブルを構築できます。 [@tanstack/react-table](https://tanstack.com/table/latest) と組み合わせて、並べ替え、フィルタリング、ページネーションを備えたテーブルを作成します。

詳細については、[データ テーブル](/docs/components/aria/data-table) ドキュメントを参照してください。

[タスク](/examples/tasks) デモでデータ テーブルの例を確認することもできます。

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="table-rtl"
  direction="rtl"
  previewClassName="h-auto"
/>

## API リファレンス

詳細については、[React Aria Table](https://react-aria.adobe.com/Table#api) ドキュメントを参照してください。
