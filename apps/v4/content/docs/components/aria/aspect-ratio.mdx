---
title: アスペクト比
description: コンテンツを希望の比率で表示します。
base: aria
component: true
---

<ComponentPreview name="aspect-ratio-demo" styleName="aria-nova" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add aspect-ratio
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="aspect-ratio"
  title="components/ui/aspect-ratio.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import { AspectRatio } from "@/components/ui/aspect-ratio"
```

```tsx showLineNumbers
<AspectRatio ratio={16 / 9}>
  <Image src="..." alt="Image" className="rounded-md object-cover" />
</AspectRatio>
```

## 正方形

`ratio={1 / 1}` プロップを使用した正方形のアスペクト比コンポーネント。画像を正方形で表示する場合に便利です。

<ComponentPreview name="aspect-ratio-square" styleName="aria-nova" />

## 縦長

`ratio={9 / 16}` プロップを使用した縦長のアスペクト比コンポーネント。これは、画像を縦長形式で表示する場合に便利です。

<ComponentPreview
  styleName="aria-nova"
  name="aspect-ratio-portrait"
  previewClassName="h-96"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="aspect-ratio-rtl"
  direction="rtl"
  previewClassName="h-96"
/>

## API リファレンス

### アスペクト比

`AspectRatio` コンポーネントは、希望の比率でコンテンツを表示します。

| プロップ | タイプ | デフォルト | 必須 |
| ----------- | -------- | ------- | -------- |
| `ratio` | `number` | - |はい |
| `className` | `string` | - |いいえ |
