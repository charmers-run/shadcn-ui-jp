---
title: ラジオグループ
description: ラジオボタンと呼ばれる、同時に1つまで選択できるチェック可能なボタンのセット。
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/RadioGroup
  api: https://react-aria.adobe.com/RadioGroup#api
---

<ComponentPreview styleName="aria-nova" name="radio-group-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add radio-group
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="radio-group"
  title="components/ui/radio-group.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import { Label } from "@/components/ui/label"
import { RadioGroup, RadioGroupItem } from "@/components/ui/radio-group"
```

```tsx showLineNumbers
<RadioGroup defaultValue="option-one">
  <div className="flex items-center gap-3">
    <RadioGroupItem value="option-one" id="option-one" />
    <Label htmlFor="option-one">Option One</Label>
  </div>
  <div className="flex items-center gap-3">
    <RadioGroupItem value="option-two" id="option-two" />
    <Label htmlFor="option-two">Option Two</Label>
  </div>
</RadioGroup>
```

## 構成

次の構成を使用して、`RadioGroup` を構築します。

```text
RadioGroup
├── RadioGroupItem
└── RadioGroupItem
```

## 説明

`Field` コンポーネントを使用して、説明付きのラジオグループ項目を表示します。

<ComponentPreview styleName="aria-nova" name="radio-group-description" />

## 選択カード

`FieldLabel` を使用して `Field` 全体をラップし、クリック可能なカードスタイルの選択を行います。

<ComponentPreview styleName="aria-nova" name="radio-group-choice-card" />

## フィールドセット

`FieldSet` と `FieldLegend` を使用して、ラベルと説明付きでラジオ項目をグループ化します。

<ComponentPreview styleName="aria-nova" name="radio-group-fieldset" />

## 無効

`RadioGroup` で `isDisabled` プロパティを使用して、すべての項目を無効にします。

<ComponentPreview styleName="aria-nova" name="radio-group-disabled" />

## 無効な状態

`RadioGroupItem` では `isInvalid` を、`Field` では `data-invalid` を使用して検証エラーを表示します。

<ComponentPreview styleName="aria-nova" name="radio-group-invalid" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="radio-group-rtl"
  direction="rtl"
/>

## API リファレンス

詳細については、[React Aria Radio Group](https://react-aria.adobe.com/RadioGroup#api) のドキュメントを参照してください。
