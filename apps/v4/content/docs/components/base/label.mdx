---
title: ラベル
description: コントロールに関連付けられたアクセス可能なラベルをレンダリングします。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/label
  api: https://base-ui.com/react/components/label#api-reference
---

<ComponentPreview styleName="base-nova" name="label-demo" />

<Callout>
  フォームフィールドには、組み込みのラベル、説明、エラー処理を含む
  [Field](/docs/components/base/field) コンポーネントを使用してください。
</Callout>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add label
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install @base-ui/react
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="label"
  title="components/ui/label.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx
import { Label } from "@/components/ui/label"
```

```tsx
<Label htmlFor="email">Your email address</Label>
```

## フィールド

フォームフィールドには、`FieldLabel`、`FieldDescription`、`FieldError` などが組み込まれた
[Field](/docs/components/base/field) コンポーネントを使用してください。

```tsx
<Field>
  <FieldLabel htmlFor="email">Your email address</FieldLabel>
  <Input id="email" />
</Field>
```

<ComponentPreview
  styleName="base-nova"
  name="field-demo"
  previewClassName="h-[44rem]"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview styleName="base-nova" name="label-rtl" direction="rtl" />

## API リファレンス

詳細については、[ベース UI ラベル](https://base-ui.com/react/components/label#api-reference) ドキュメントを参照してください。
