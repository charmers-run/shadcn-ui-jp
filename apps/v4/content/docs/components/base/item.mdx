---
title: アイテム
description: メディア、タイトル、説明、アクションを含むコンテンツを表示するための多用途コンポーネント。
base: base
component: true
---

<ComponentPreview styleName="base-nova" name="item-demo" />

`Item` コンポーネントは、ほぼすべての種類のコンテンツを格納できる簡単なフレックス コンテナーです。タイトル、説明、アクションを表示するために使用します。これを `ItemGroup` コンポーネントとグループ化して、項目のリストを作成します。

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add item
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="item"
  title="components/ui/item.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import {
  Item,
  ItemActions,
  ItemContent,
  ItemDescription,
  ItemMedia,
  ItemTitle,
} from "@/components/ui/item"
```

```tsx showLineNumbers
<Item>
  <ItemMedia variant="icon">
    <Icon />
  </ItemMedia>
  <ItemContent>
    <ItemTitle>Title</ItemTitle>
    <ItemDescription>Description</ItemDescription>
  </ItemContent>
  <ItemActions>
    <Button>Action</Button>
  </ItemActions>
</Item>
```

## 構成

次の構成を使用して `Item` を構築します。

```text
ItemGroup
└── Item
    ├── ItemHeader
    ├── ItemMedia
    ├── ItemContent
    │   ├── ItemTitle
    │   └── ItemDescription
    ├── ItemActions
    └── ItemFooter
```

## 項目とフィールド

チェックボックス、入力、ラジオ、または選択などのフォーム入力を表示する必要がある場合は、`Field` を使用します。

タイトル、説明、アクションなどのコンテンツのみを表示する場合は、`Item` を使用します。

## バリエーション

項目の視覚スタイルを変更するには、`variant` プロップを使用します。

<ComponentPreview
  styleName="base-nova"
  name="item-variant"
  previewClassName="h-96"
/>

## サイズ

項目のサイズを変更するには、`size` プロップを使用します。利用可能なサイズは `default`、`sm`、`xs` です。

<ComponentPreview
  styleName="base-nova"
  name="item-size"
  previewClassName="h-96"
/>

## アイコン

アイコンを表示するには、`ItemMedia` を `variant="icon"` とともに使用します。

<ComponentPreview styleName="base-nova" name="item-icon" />

## アバター

`ItemMedia` を `variant="avatar"` とともに使用して、アバターを表示できます。

<ComponentPreview styleName="base-nova" name="item-avatar" />

## 画像

画像を表示するには、`ItemMedia` を `variant="image"` とともに使用します。

<ComponentPreview styleName="base-nova" name="item-image" />

## グループ

`ItemGroup` を使用して、関連する項目をグループ化します。

<ComponentPreview
  styleName="base-nova"
  name="item-group"
  previewClassName="h-96"
/>

## ヘッダー

`ItemHeader` を使用して、項目コンテンツの上にヘッダーを追加します。

<ComponentPreview
  styleName="base-nova"
  name="item-header"
  previewClassName="h-96"
/>

## リンク

`render` プロップを使用して項目をリンクとしてレンダリングします。ホバー状態とフォーカス状態はアンカー要素に適用されます。

<ComponentPreview styleName="base-nova" name="item-link" />

```tsx showLineNumbers
<Item render={<a href="/dashboard" />}>
  <ItemMedia variant="icon">
    <HomeIcon />
  </ItemMedia>
  <ItemContent>
    <ItemTitle>Dashboard</ItemTitle>
    <ItemDescription>Overview of your account and activity.</ItemDescription>
  </ItemContent>
</Item>
```

## ドロップダウン

<ComponentPreview styleName="base-nova" name="item-dropdown" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview styleName="base-nova" name="item-rtl" direction="rtl" />

## API リファレンス

### アイテム

メディア、タイトル、説明、およびアクションを含むコンテンツを表示するための主要コンポーネント。

|プロップ |タイプ |デフォルト |
| --------- | ----------------------------------- | ----------- |
| `variant` | `"default" \| "outline" \| "muted"` | `"default"` |
| `size` | `"default" \| "sm" \| "xs"` | `"default"` |
| `render` | `React.ReactElement` |             |

### ItemGroup

関連項目を一貫したスタイルでグループ化するコンテナー。

```tsx
<ItemGroup>
  <Item />
  <Item />
</ItemGroup>
```

### ItemSeparator

グループ内の項目間の区切り文字。

```tsx
<ItemGroup>
  <Item />
  <ItemSeparator />
  <Item />
</ItemGroup>
```

### アイテムメディア

`ItemMedia` を使用して、アイコン、画像、アバターなどのメディア コンテンツを表示します。

|プロップ |タイプ |デフォルト |
| --------- | -------------------------------- | ----------- |
| `variant` | `"default" \| "icon" \| "image"` | `"default"` |

```tsx
<ItemMedia variant="icon">
  <Icon />
</ItemMedia>
```

```tsx
<ItemMedia variant="image">
  <img src="..." alt="..." />
</ItemMedia>
```

### ItemContent

アイテムのタイトルと説明をラップします。

```tsx
<ItemContent>
  <ItemTitle>Title</ItemTitle>
  <ItemDescription>Description</ItemDescription>
</ItemContent>
```

### ItemTitle

項目のタイトルを表示します。

```tsx
<ItemTitle>Item Title</ItemTitle>
```

### 項目説明

項目の説明を表示します。

```tsx
<ItemDescription>Item description</ItemDescription>
```

### ItemActions

アクション ボタンまたはその他のインタラクティブ要素のコンテナー。

```tsx
<ItemActions>
  <Button>Action</Button>
</ItemActions>
```

### ItemHeader

項目コンテンツの上にヘッダーを表示します。

```tsx
<Item>
  <ItemHeader>Header</ItemHeader>
  <ItemContent>...</ItemContent>
</Item>
```

### アイテムフッター

項目コンテンツの下にフッターを表示します。

```tsx
<Item>
  <ItemContent>...</ItemContent>
  <ItemFooter>Footer</ItemFooter>
</Item>
```
