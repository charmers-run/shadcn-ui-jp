---
title: コンテキスト メニュー
description: 右クリックによってトリガーされるアクションのメニューを表示します。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/context-menu
  api: https://base-ui.com/react/components/context-menu#api-reference
---

<ComponentPreview
  styleName="base-nova"
  name="context-menu-demo"
  description="サブメニュー項目を含むコンテキスト メニュー。"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add context-menu
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install @base-ui/react
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="context-menu"
  title="components/ui/context-menu.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import {
  ContextMenu,
  ContextMenuContent,
  ContextMenuItem,
  ContextMenuTrigger,
} from "@/components/ui/context-menu"
```

```tsx showLineNumbers
<ContextMenu>
  <ContextMenuTrigger>Right click here</ContextMenuTrigger>
  <ContextMenuContent>
    <ContextMenuItem>Profile</ContextMenuItem>
    <ContextMenuItem>Billing</ContextMenuItem>
    <ContextMenuItem>Team</ContextMenuItem>
    <ContextMenuItem>Subscription</ContextMenuItem>
  </ContextMenuContent>
</ContextMenu>
```

## 構成

次の構成を使用して `ContextMenu` を作成します。

```text
ContextMenu
├── ContextMenuTrigger
└── ContextMenuContent
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   ├── ContextMenuItem
    │   └── ContextMenuItem
    ├── ContextMenuSeparator
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   ├── ContextMenuCheckboxItem
    │   └── ContextMenuCheckboxItem
    ├── ContextMenuSeparator
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   └── ContextMenuRadioGroup
    │       ├── ContextMenuRadioItem
    │       └── ContextMenuRadioItem
    └── ContextMenuSub
        ├── ContextMenuSubTrigger
        └── ContextMenuSubContent
            └── ContextMenuGroup
                ├── ContextMenuItem
                └── ContextMenuItem
```

## 基本

いくつかのアクションを備えたシンプルなコンテキスト メニュー。

<ComponentPreview styleName="base-nova" name="context-menu-basic" />

## サブメニュー

二次アクションをネストするには `ContextMenuSub` を使用します。

<ComponentPreview styleName="base-nova" name="context-menu-submenu" />

## ショートカット

キーボード ヒントを表示するには、`ContextMenuShortcut` を追加します。

<ComponentPreview styleName="base-nova" name="context-menu-shortcuts" />

## グループ

関連するアクションをグループ化し、区切り線で区切ります。

<ComponentPreview styleName="base-nova" name="context-menu-groups" />

## アイコン

アイコンとラベルを組み合わせて、すばやくスキャンします。

<ComponentPreview styleName="base-nova" name="context-menu-icons" />

## チェックボックス

切り替えには `ContextMenuCheckboxItem` を使用します。

<ComponentPreview styleName="base-nova" name="context-menu-checkboxes" />

## ラジオ

排他的な選択には `ContextMenuRadioItem` を使用します。

<ComponentPreview styleName="base-nova" name="context-menu-radio" />

## 破壊的

`variant="destructive"` を使用して、メニュー項目を破壊的なスタイルに設定します。

<ComponentPreview styleName="base-nova" name="context-menu-destructive" />

## 配置

`side` および `align` プロパティを使用して、サブメニューの配置を制御します。

<ComponentPreview styleName="base-nova" name="context-menu-sides" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="base-nova"
  name="context-menu-rtl"
  direction="rtl"
/>

`side="inline-end"` を使用して、トリガーの論理終了側にメニューを配置します。

```tsx showLineNumbers
<ContextMenu>
  <ContextMenuTrigger>Right click here</ContextMenuTrigger>
  <ContextMenuContent side="inline-end">
    <ContextMenuItem>Profile</ContextMenuItem>
    <ContextMenuItem>Billing</ContextMenuItem>
    <ContextMenuItem>Team</ContextMenuItem>
    <ContextMenuItem>Subscription</ContextMenuItem>
  </ContextMenuContent>
</ContextMenu>
```

## API リファレンス

詳細については、[ベース UI](https://base-ui.com/react/components/context-menu#api-reference) ドキュメントを参照してください。
