---
title: コンボボックス
description: 候補のリストを含むオートコンプリート入力。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/combobox
  api: https://base-ui.com/react/components/combobox#api-reference
---

<ComponentPreview
  styleName="base-nova"
  name="combobox-demo"
  description="フレームワークのリストが含まれるコンボボックス。"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add combobox
```

</TabsContent>
<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install @base-ui/react
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="combobox"
  title="components/ui/combobox.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import {
  Combobox,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
} from "@/components/ui/combobox"
```

```tsx showLineNumbers
const frameworks = ["Next.js", "SvelteKit", "Nuxt.js", "Remix", "Astro"]

export function ExampleCombobox() {
  return (
    <Combobox items={frameworks}>
      <ComboboxInput placeholder="Select a framework" />
      <ComboboxContent>
        <ComboboxEmpty>No items found.</ComboboxEmpty>
        <ComboboxList>
          {(item) => (
            <ComboboxItem key={item} value={item}>
              {item}
            </ComboboxItem>
          )}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  )
}
```

## 構成

### シンプル

単一行入力とフラット リスト ([基本](#basic) を参照)。

```text
Combobox
├── ComboboxInput
└── ComboboxContent
    ├── ComboboxEmpty
    └── ComboboxList
        ├── ComboboxItem
        └── ComboboxItem
```

### チップ付き

`multiple`、チップ、およびチップ入力による複数選択 ([複数](#multiple) を参照)。

```text
Combobox
├── ComboboxChips
│   ├── ComboboxValue
│   │   └── ComboboxChip
│   └── ComboboxChipsInput
└── ComboboxContent
    ├── ComboboxEmpty
    └── ComboboxList
        ├── ComboboxItem
        └── ComboboxItem
```

### グループとコレクションを使用する

各 `ComboboxGroup` 内で `ComboboxCollection` を使用し、グループ間に区切り文字を使用してグループごとに項目をネストします ([グループ](#groups) を参照)。

```text
Combobox
├── ComboboxInput
└── ComboboxContent
    ├── ComboboxEmpty
    └── ComboboxList
        ├── ComboboxGroup
        │   ├── ComboboxLabel
        │   └── ComboboxCollection
        │       ├── ComboboxItem
        │       └── ComboboxItem
        ├── ComboboxSeparator
        └── ComboboxGroup
            ├── ComboboxLabel
            └── ComboboxCollection
                ├── ComboboxItem
                └── ComboboxItem
```

## カスタム項目

アイテムがオブジェクトの場合は `itemToStringValue` を使用します。

```tsx showLineNumbers
import * as React from "react"

import {
  Combobox,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
} from "@/components/ui/combobox"

type Framework = {
  label: string
  value: string
}

const frameworks: Framework[] = [
  { label: "Next.js", value: "next" },
  { label: "SvelteKit", value: "sveltekit" },
  { label: "Nuxt", value: "nuxt" },
]

export function ExampleComboboxCustomItems() {
  return (
    <Combobox
      items={frameworks}
      itemToStringValue={(framework) => framework.label}
    >
      <ComboboxInput placeholder="Select a framework" />
      <ComboboxContent>
        <ComboboxEmpty>No items found.</ComboboxEmpty>
        <ComboboxList>
          {(framework) => (
            <ComboboxItem key={framework.value} value={framework}>
              {framework.label}
            </ComboboxItem>
          )}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  )
}
```

## 複数選択

複数選択動作のチップでは `multiple` を使用します。

```tsx showLineNumbers
import * as React from "react"

import {
  Combobox,
  ComboboxChip,
  ComboboxChips,
  ComboboxChipsInput,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
  ComboboxValue,
} from "@/components/ui/combobox"

const frameworks = ["Next.js", "SvelteKit", "Nuxt.js", "Remix", "Astro"]

export function ExampleComboboxMultiple() {
  const [value, setValue] = React.useState<string[]>([])

  return (
    <Combobox
      items={frameworks}
      multiple
      value={value}
      onValueChange={setValue}
    >
      <ComboboxChips>
        <ComboboxValue>
          {value.map((item) => (
            <ComboboxChip key={item}>{item}</ComboboxChip>
          ))}
        </ComboboxValue>
        <ComboboxChipsInput placeholder="Add framework" />
      </ComboboxChips>
      <ComboboxContent>
        <ComboboxEmpty>No items found.</ComboboxEmpty>
        <ComboboxList>
          {(item) => (
            <ComboboxItem key={item} value={item}>
              {item}
            </ComboboxItem>
          )}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  )
}
```

## 基本

フレームワークのリストを含む単純なコンボボックス。

<ComponentPreview styleName="base-nova" name="combobox-basic" />

## 複数

`multiple` と `ComboboxChips` を使用した複数選択のコンボボックス。

<ComponentPreview styleName="base-nova" name="combobox-multiple" />

## クリア ボタン

`showClear` プロップを使用してクリア ボタンを表示します。

<ComponentPreview styleName="base-nova" name="combobox-clear" />

## グループ

`ComboboxGroup` および `ComboboxSeparator` を使用して項目をグループ化します。

<ComponentPreview styleName="base-nova" name="combobox-groups" />

## カスタム項目

`ComboboxItem` 内でカスタム コンポーネントをレンダリングできます。

<ComponentPreview styleName="base-nova" name="combobox-custom" />

## 無効な状態

コンボボックスを無効な状態にするには、`aria-invalid` プロパティを使用します。

<ComponentPreview styleName="base-nova" name="combobox-invalid" />

## 無効

`disabled` プロパティを使用してコンボボックスを無効にします。

<ComponentPreview styleName="base-nova" name="combobox-disabled" />

## 自動ハイライト

`autoHighlight` プロップを使用して、フィルターの最初の項目を自動的に強調表示します。

<ComponentPreview styleName="base-nova" name="combobox-auto-highlight" />

## ポップアップ

`render` プロップを使用すると、ボタンまたはその他のコンポーネントからコンボボックスをトリガーできます。`ComboboxInput` を `ComboboxContent` 内に移動します。

<ComponentPreview styleName="base-nova" name="combobox-popup" />

## 入力グループ

`ComboboxInput` 内の `InputGroupAddon` コンポーネントを使用して、コンボボックスにアドオンを追加できます。

<ComponentPreview styleName="base-nova" name="combobox-input-group" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="base-nova"
  name="combobox-rtl"
  direction="rtl"
  align="start"
/>

## API リファレンス

詳細については、[ベース UI](https://base-ui.com/react/components/combobox#api-reference) ドキュメントを参照してください。
