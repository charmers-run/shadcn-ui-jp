---
title: バッジ
description: バッジまたはバッジに似たコンポーネントを表示します。
base: base
component: true
---

<ComponentPreview styleName="base-nova" name="badge-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add badge
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="badge"
  title="components/ui/badge.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx
import { Badge } from "@/components/ui/badge"
```

```tsx
<Badge variant="default | outline | secondary | destructive">Badge</Badge>
```

## バリアント

`variant` プロップを使用して、バッジのバリアントを変更します。

<ComponentPreview styleName="base-nova" name="badge-variants" />

## アイコン付き

バッジ内にアイコンをレンダリングできます。左側のアイコンをレンダリングするには `data-icon="inline-start"` を使用し、右側のアイコンをレンダリングするには `data-icon="inline-end"` を使用します。

<ComponentPreview styleName="base-nova" name="badge-icon" />

## スピナー付き

バッジ内にスピナーをレンダリングできます。 `data-icon="inline-start"` または `data-icon="inline-end"` プロップをスピナーに忘れずに追加してください。

<ComponentPreview styleName="base-nova" name="badge-spinner" />

## リンク

リンクをバッジとしてレンダリングするには、`render` プロップを使用します。

<ComponentPreview styleName="base-nova" name="badge-link" />

## カスタムカラー

`bg-green-50 dark:bg-green-800` などのカスタム クラスを `Badge` コンポーネントに追加することで、バッジの色をカスタマイズできます。

<ComponentPreview styleName="base-nova" name="badge-colors" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview styleName="base-nova" name="badge-rtl" direction="rtl" />

## API リファレンス

### バッジ

`Badge` コンポーネントは、バッジまたはバッジに似たコンポーネントを表示します。

|小道具 |タイプ |デフォルト |
| ----------- | ----------------------------------------------------------------------------- | ----------- |
| `variant`   | `"default" \| "secondary" \| "destructive" \| "outline" \| "ghost" \| "link"` | `"default"` |
| `className` | `string`                                                                      | -           |
