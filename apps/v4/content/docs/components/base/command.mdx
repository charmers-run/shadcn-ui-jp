---
title: コマンド
description: 検索とクイック アクションのためのコマンド メニュー。
base: base
component: true
links:
  doc: https://github.com/dip/cmdk
---

<ComponentPreview
  styleName="base-nova"
  name="command-demo"
  align="start"
  previewClassName="h-[24.5rem]"
/>

## 概要

`<Command />` コンポーネントは、[Dip](https://www.dip.org/) による [`cmdk`](https://github.com/dip/cmdk) コンポーネントを使用します。

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add command
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install cmdk
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="command"
  title="components/ui/command.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import {
  Command,
  CommandDialog,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
  CommandShortcut,
} from "@/components/ui/command"
```

```tsx showLineNumbers
<Command className="max-w-sm rounded-lg border">
  <CommandInput placeholder="Type a command or search..." />
  <CommandList>
    <CommandEmpty>No results found.</CommandEmpty>
    <CommandGroup heading="Suggestions">
      <CommandItem>Calendar</CommandItem>
      <CommandItem>Search Emoji</CommandItem>
      <CommandItem>Calculator</CommandItem>
    </CommandGroup>
    <CommandSeparator />
    <CommandGroup heading="Settings">
      <CommandItem>Profile</CommandItem>
      <CommandItem>Billing</CommandItem>
      <CommandItem>Settings</CommandItem>
    </CommandGroup>
  </CommandList>
</Command>
```

## 構成

次の構成を使用して `Command` を作成します。

```text
Command
├── CommandInput
└── CommandList
    ├── CommandEmpty
    ├── CommandGroup
    │   ├── CommandItem
    │   └── CommandItem
    ├── CommandSeparator
    └── CommandGroup
        ├── CommandItem
        └── CommandItem
```

## 基本

ダイアログ内の単純なコマンド メニュー。

<ComponentPreview styleName="base-nova" name="command-basic" />

## ショートカット

<ComponentPreview styleName="base-nova" name="command-shortcuts" />

## グループ

グループ、アイコン、区切り文字を含むコマンド メニュー。

<ComponentPreview styleName="base-nova" name="command-groups" />

## スクロール可能

複数の項目を含むスクロール可能なコマンド メニュー。

<ComponentPreview styleName="base-nova" name="command-scrollable" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="base-nova"
  name="command-rtl"
  direction="rtl"
  align="start"
  previewClassName="h-[24.5rem]"
/>

## API リファレンス

詳細については、[cmdk](https://github.com/dip/cmdk) のドキュメントを参照してください。
