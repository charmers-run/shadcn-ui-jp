---
title: 入力 OTP
description: コピー＆ペースト機能を備えたアクセス可能なワンタイム パスワード コンポーネント。
base: base
component: true
links:
  doc: https://input-otp.rodz.dev
---

<ComponentPreview styleName="base-nova" name="input-otp-demo" />

## 概要

入力 OTP は、[@guilherme_rodz](https://twitter.com/guilherme_rodz) によって [input-otp](https://github.com/guilhermerodz/input-otp) の上に構築されます。

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add input-otp
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install input-otp
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="input-otp"
  title="components/ui/input-otp.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import {
  InputOTP,
  InputOTPGroup,
  InputOTPSeparator,
  InputOTPSlot,
} from "@/components/ui/input-otp"
```

```tsx showLineNumbers
<InputOTP maxLength={6}>
  <InputOTPGroup>
    <InputOTPSlot index={0} />
    <InputOTPSlot index={1} />
    <InputOTPSlot index={2} />
  </InputOTPGroup>
  <InputOTPSeparator />
  <InputOTPGroup>
    <InputOTPSlot index={3} />
    <InputOTPSlot index={4} />
    <InputOTPSlot index={5} />
  </InputOTPGroup>
</InputOTP>
```

## 構成

次の構成を使用して `InputOTP` を構築します。

```text
InputOTP
├── InputOTPGroup
│   ├── InputOTPSlot
│   ├── InputOTPSlot
│   └── InputOTPSlot
├── InputOTPSeparator
├── InputOTPGroup
│   ├── InputOTPSlot
│   ├── InputOTPSlot
│   └── InputOTPSlot
├── InputOTPSeparator
└── InputOTPGroup
    ├── InputOTPSlot
    └── InputOTPSlot
```

## パターン

`pattern` プロパティを使用して、OTP 入力のカスタム パターンを定義します。

```tsx showLineNumbers {1,5}
import { REGEXP_ONLY_DIGITS_AND_CHARS } from "input-otp"

;<InputOTP maxLength={6} pattern={REGEXP_ONLY_DIGITS_AND_CHARS}>
  ...
</InputOTP>
```

<ComponentPreview styleName="base-nova" name="input-otp-pattern" />

## 区切り文字

`<InputOTPSeparator />` コンポーネントを使用して、入力グループ間に区切り文字を追加します。

<ComponentPreview styleName="base-nova" name="input-otp-separator" />

## 無効

`disabled` プロパティを使用して入力を無効にします。

<ComponentPreview styleName="base-nova" name="input-otp-disabled" />

## 制御

`value` および `onChange` プロパティを使用して入力値を制御します。

<ComponentPreview styleName="base-nova" name="input-otp-controlled" />

## 無効です

エラー状態を表示するには、スロットで `aria-invalid` を使用します。

<ComponentPreview styleName="base-nova" name="input-otp-invalid" />

## 4 桁

PIN コードの一般的なパターン。これは `pattern={REGEXP_ONLY_DIGITS}` プロパティを使用します。

<ComponentPreview styleName="base-nova" name="input-otp-four-digits" />

## 英数字

文字と数字の両方を受け入れるには、`REGEXP_ONLY_DIGITS_AND_CHARS` を使用します。

<ComponentPreview styleName="base-nova" name="input-otp-alphanumeric" />

## フォーム

<ComponentPreview
  styleName="base-nova"
  name="input-otp-form"
  previewClassName="h-[30rem]"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview styleName="base-nova" name="input-otp-rtl" direction="rtl" />

## API リファレンス

詳細については、[input-otp](https://input-otp.rodz.dev) ドキュメントを参照してください。
