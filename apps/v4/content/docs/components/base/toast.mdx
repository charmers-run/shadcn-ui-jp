---
title: トースト
description: 一時的に表示される簡潔なメッセージ。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/toast
  api: https://base-ui.com/react/components/toast#api-reference
---

<ComponentPreview styleName="base-nova" name="toast-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">マニュアル</TabsTrigger>
</TabsList>
<TabsContent value="cli">

<Steps className="mb-0 pt-2">

<Step>次のコマンドを実行します。</Step>

```bash
npx shadcn@latest add toast
```

<Step>トースター コンポーネントを追加します。</Step>

```tsx title="app/layout.tsx" {1,9} showLineNumbers
import { Toaster } from "@/components/ui/toast"

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <main>{children}</main>
        <Toaster />
      </body>
    </html>
  )
}
```

</Steps>

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします:</Step>

```bash
npm install @base-ui/react
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="toast"
  title="components/ui/toast.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

<Step>トースター コンポーネントを追加します。</Step>

```tsx title="app/layout.tsx" {1,8} showLineNumbers
import { Toaster } from "@/components/ui/toast"

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
        <Toaster />
      </body>
    </html>
  )
}
```

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx
import { toast } from "@/components/ui/toast"
```

```tsx
toast.add({
  title: "Event created",
  description: "Sunday, December 3 at 9:00 AM",
})
```

## 種類

ステータス アイコンを表示するには、`type` オプションを設定します。組み込みレンダラーは `success`、`info`、`warning`、`error`、`loading` を認識します。

<ComponentPreview styleName="base-nova" name="toast-types" />

## アクション

`actionProps` にボタンのプロップを渡すと、アクションがレンダリングされます。

```tsx
const id = toast.add({
  title: "Event created",
  actionProps: {
    children: "Undo",
    onClick() {
      toast.close(id)
    },
  },
})
```

## プロミス

`toast.promise` を使用すると、非同期タスクが読み込み中、成功、エラーの各状態へと移行するのに合わせて、1 つのトーストを更新できます。

<ComponentPreview styleName="base-nova" name="toast-promise" />

## API リファレンス

マネージャー オプション、スタッキング、スワイプによる閉じ方、プリミティブ API の詳細については、[Base UI Toast ドキュメント](https://base-ui.com/react/components/toast) を参照してください。
