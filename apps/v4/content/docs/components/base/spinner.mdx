---
title: スピナー
description: 読み込み状態を示すために使用できるインジケーター。
base: base
component: true
---

<ComponentPreview styleName="base-nova" name="spinner-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">マニュアル</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add spinner
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="spinner"
  title="components/ui/spinner.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx
import { Spinner } from "@/components/ui/spinner"
```

```tsx
<Spinner />
```

## カスタマイズ

`Spinner` コンポーネントを編集すると、デフォルトのスピナー アイコンを他のアイコンに置き換えることができます。

<ComponentPreview styleName="base-nova" name="spinner-custom" />

```tsx showLineNumbers title="components/ui/spinner.tsx"
import { cn } from "cn"
import { LoaderIcon } from "lucide-react"

function Spinner({ className, ...props }: React.ComponentProps<"svg">) {
  return (
    <LoaderIcon
      role="status"
      aria-label="Loading"
      className={cn("size-4 animate-spin", className)}
      {...props}
    />
  )
}

export { Spinner }
```

## サイズ

`size-*` ユーティリティ クラスを使用して、スピナーのサイズを変更します。

<ComponentPreview styleName="base-nova" name="spinner-size" />

## ボタン

読み込み状態を示すスピナーをボタンに追加します。 `<Spinner />` を開始位置として `data-icon="inline-start"` を指定してラベルの前に配置するか、終了位置として `data-icon="inline-end"` を指定してラベルの後に配置します。

<ComponentPreview styleName="base-nova" name="spinner-button" />

## バッジ

読み込み状態を示すスピナーをバッジに追加します。 `<Spinner />` を開始位置として `data-icon="inline-start"` を指定してラベルの前に配置するか、終了位置として `data-icon="inline-end"` を指定してラベルの後に配置します。

<ComponentPreview styleName="base-nova" name="spinner-badge" />

## 入力グループ

<ComponentPreview styleName="base-nova" name="spinner-input-group" />

## 空

<ComponentPreview styleName="base-nova" name="spinner-empty" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview styleName="base-nova" name="spinner-rtl" direction="rtl" />
