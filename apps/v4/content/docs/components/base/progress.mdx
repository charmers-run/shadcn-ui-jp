---
title: 進行状況
description: タスクの完了の進行状況を示すインジケーターを表示します。通常は進行状況バーとして表示されます。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/progress
  api: https://base-ui.com/react/components/progress#api-reference
---

<ComponentPreview styleName="base-nova" name="progress-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add progress
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install @base-ui/react
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="progress"
  title="components/ui/progress.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import { Progress } from "@/components/ui/progress"
```

```tsx showLineNumbers
<Progress value={33} />
```

## 構成

### ラベルと値付き

`ProgressLabel` および `ProgressValue` を使用して、ラベルと値の表示を追加します。

```tsx showLineNumbers
import {
  Progress,
  ProgressLabel,
  ProgressValue,
} from "@/components/ui/progress"

;<Progress value={56} className="w-full max-w-sm">
  <ProgressLabel>Upload progress</ProgressLabel>
  <ProgressValue />
</Progress>
```

```text
Progress
├── ProgressLabel
├── ProgressValue
└── ProgressTrack
    └── ProgressIndicator
```

## ラベル

`ProgressLabel` および `ProgressValue` を使用して、ラベルと値の表示を追加します。

<ComponentPreview styleName="base-nova" name="progress-label" />

## 制御

スライダーで制御できる進行状況バー。

<ComponentPreview styleName="base-nova" name="progress-controlled" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview styleName="base-nova" name="progress-rtl" direction="rtl" />

## API リファレンス

詳細については、[Base UI Progress](https://base-ui.com/react/components/progress#api-reference) ドキュメントを参照してください。
