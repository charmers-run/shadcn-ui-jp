---
title: スクロール領域
description: カスタムのクロスブラウザースタイルのために、ネイティブのスクロール機能を拡張します。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/scroll-area
  api: https://base-ui.com/react/components/scroll-area#api-reference
---

<ComponentPreview
  styleName="base-nova"
  name="scroll-area-demo"
  previewClassName="h-96"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add scroll-area
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install @base-ui/react
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="scroll-area"
  title="components/ui/scroll-area.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import { ScrollArea, ScrollBar } from "@/components/ui/scroll-area"
```

```tsx showLineNumbers
<ScrollArea className="h-[200px] w-[350px] rounded-md border p-4">
  Your scrollable content here.
</ScrollArea>
```

## 構成

次の構成を使用して、`ScrollArea` を構築します。

```text
ScrollArea
└── ScrollBar
```

## 水平方向

水平スクロールには `ScrollBar` を `orientation="horizontal"` とともに使用します。

<ComponentPreview styleName="base-nova" name="scroll-area-horizontal-demo" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview
  styleName="base-nova"
  name="scroll-area-rtl"
  direction="rtl"
  previewClassName="h-auto"
/>

## API リファレンス

詳細については、[Base UI Scroll Area](https://base-ui.com/react/components/scroll-area#api-reference) のドキュメントを参照してください。
