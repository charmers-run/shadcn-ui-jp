---
title: スライダー
description: 指定した範囲からユーザーが値を選択する入力。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/slider
  api: https://base-ui.com/react/components/slider#api-reference
---

<ComponentPreview styleName="base-nova" name="slider-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add slider
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install @base-ui/react
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="slider"
  title="components/ui/slider.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx
import { Slider } from "@/components/ui/slider"
```

```tsx
<Slider defaultValue={[33]} max={100} step={1} />
```

## 範囲

範囲スライダーには 2 つの値を持つ配列を使用します。

<ComponentPreview styleName="base-nova" name="slider-range" />

## 複数のサム

複数のサムに複数の値を含む配列を使用します。

<ComponentPreview styleName="base-nova" name="slider-multiple" />

## 垂直

垂直スライダーには `orientation="vertical"` を使用します。

<ComponentPreview styleName="base-nova" name="slider-vertical" />

## 制御

<ComponentPreview styleName="base-nova" name="slider-controlled" />

## 無効

`disabled` プロップを使用してスライダーを無効にします。

<ComponentPreview styleName="base-nova" name="slider-disabled" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview styleName="base-nova" name="slider-rtl" direction="rtl" />

## API リファレンス

[Base UI Slider](https://base-ui.com/react/components/slider#api-reference) のドキュメントを参照してください。
