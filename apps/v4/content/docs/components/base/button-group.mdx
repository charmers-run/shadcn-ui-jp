---
title: ボタングループ
description: 関連するボタンを一貫したスタイルでグループ化するコンテナー。
base: base
component: true
---

<ComponentPreview styleName="base-nova" name="button-group-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add button-group
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install @base-ui/react
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="button-group"
  title="components/ui/button-group.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx
import {
  ButtonGroup,
  ButtonGroupSeparator,
  ButtonGroupText,
} from "@/components/ui/button-group"
```

```tsx
<ButtonGroup>
  <Button>Button 1</Button>
  <Button>Button 2</Button>
</ButtonGroup>
```

## 構成

`ButtonGroup` を構築するには、次の構成を使用します。

```text
ButtonGroup
├── Button or Input
├── ButtonGroupSeparator
└── ButtonGroupText
```

## アクセシビリティ

- `ButtonGroup` コンポーネントの `role` 属性は `group` に設定されています。
- <Kbd>Tab</Kbd> を使用して、グループ内のボタン間を移動します。
- ボタングループにラベルを付けるには、`aria-label` または `aria-labelledby` を使用します。

```tsx showLineNumbers
<ButtonGroup aria-label="Button group">
  <Button>Button 1</Button>
  <Button>Button 2</Button>
</ButtonGroup>
```

## ButtonGroup と ToggleGroup の比較

- アクションを実行するボタンをグループ化する場合は、`ButtonGroup` コンポーネントを使用します。
- 状態を切り替えるボタンをグループ化する場合は、`ToggleGroup` コンポーネントを使用します。

## 方向

ボタン グループのレイアウトを変更するには、`orientation` プロップを設定します。

<ComponentPreview styleName="base-nova" name="button-group-orientation" />

## サイズ

個々のボタンの `size` プロップを使用してボタンのサイズを制御します。

<ComponentPreview styleName="base-nova" name="button-group-size" />

## ネスト

`<ButtonGroup>` コンポーネントをネストして、間隔をあけてボタン グループを作成します。

<ComponentPreview styleName="base-nova" name="button-group-nested" />

## 区切り

`ButtonGroupSeparator` コンポーネントは、グループ内のボタンを視覚的に分割します。

バリアント `outline` のボタンには境界線があるため、セパレータは必要ありません。他のバリアントでは、視覚的な階層を改善するためにセパレーターを使用することをお勧めします。

<ComponentPreview styleName="base-nova" name="button-group-separator" />

## 分割

`ButtonGroupSeparator` で区切られた 2 つのボタンを追加して、分割ボタン グループを作成します。

<ComponentPreview styleName="base-nova" name="button-group-split" />

## 入力

`Input` コンポーネントをボタンでラップします。

<ComponentPreview styleName="base-nova" name="button-group-input" />

## 入力グループ

`InputGroup` コンポーネントをラップして、複雑な入力レイアウトを作成します。

<ComponentPreview styleName="base-nova" name="button-group-input-group" />

## ドロップダウン メニュー

`DropdownMenu` コンポーネントを使用して分割ボタン グループを作成します。

<ComponentPreview styleName="base-nova" name="button-group-dropdown" />

## 選択

`Select` コンポーネントとペアリングします。

<ComponentPreview styleName="base-nova" name="button-group-select" />

## ポップオーバー

`Popover` コンポーネントと一緒に使用します。

<ComponentPreview styleName="base-nova" name="button-group-popover" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview
  styleName="base-nova"
  name="button-group-rtl"
  direction="rtl"
/>

## API リファレンス

### ButtonGroup

`ButtonGroup` コンポーネントは、関連するボタンを一貫したスタイルでグループ化するコンテナーです。

|プロップ |タイプ |デフォルト |
| ------------- | ---------------------------- | -------------- |
| `orientation` | `"horizontal" \| "vertical"` | `"horizontal"` |

```tsx
<ButtonGroup>
  <Button>Button 1</Button>
  <Button>Button 2</Button>
</ButtonGroup>
```

複数のボタン グループをネストして、間隔のある複雑なレイアウトを作成します。詳細については、[nested](#nested) の例を参照してください。

```tsx
<ButtonGroup>
  <ButtonGroup />
  <ButtonGroup />
</ButtonGroup>
```

### ButtonGroupSeparator

`ButtonGroupSeparator` コンポーネントは、グループ内のボタンを視覚的に分割します。

|プロップ |タイプ |デフォルト |
| ------------- | ---------------------------- | ------------ |
| `orientation` | `"horizontal" \| "vertical"` | `"vertical"` |

```tsx
<ButtonGroup>
  <Button>Button 1</Button>
  <ButtonGroupSeparator />
  <Button>Button 2</Button>
</ButtonGroup>
```

### ButtonGroupText

このコンポーネントを使用して、ボタン グループ内のテキストを表示します。

| プロップ | タイプ               | デフォルト |
| -------- | -------------------- | ------- |
| `render` | `React.ReactElement` |         |

```tsx
<ButtonGroup>
  <ButtonGroupText>Text</ButtonGroupText>
  <Button>Button</Button>
</ButtonGroup>
```

`render` プロップを使用して、カスタム コンポーネントをテキスト (ラベルなど) としてレンダリングします。

```tsx showLineNumbers
import { ButtonGroupText } from "@/components/ui/button-group"
import { Label } from "@/components/ui/label"

export function ButtonGroupTextDemo() {
  return (
    <ButtonGroup>
      <ButtonGroupText render={<Label htmlFor="name" />}>Text</ButtonGroupText>
      <Input placeholder="Type something here..." id="name" />
    </ButtonGroup>
  )
}
```
