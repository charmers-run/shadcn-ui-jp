---
title: ポップオーバー
description: ボタンによってトリガーされて、ポータルにリッチコンテンツを表示します。
base: radix
component: true
links:
  doc: https://www.radix-ui.com/docs/primitives/components/popover
  api: https://www.radix-ui.com/docs/primitives/components/popover#api-reference
---

<ComponentPreview styleName="radix-nova" name="popover-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add popover
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install radix-ui
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="popover"
  title="components/ui/popover.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import {
  Popover,
  PopoverContent,
  PopoverDescription,
  PopoverHeader,
  PopoverTitle,
  PopoverTrigger,
} from "@/components/ui/popover"
```

```tsx showLineNumbers
<Popover>
  <PopoverTrigger asChild>
    <Button variant="outline">Open Popover</Button>
  </PopoverTrigger>
  <PopoverContent>
    <PopoverHeader>
      <PopoverTitle>Title</PopoverTitle>
      <PopoverDescription>Description text here.</PopoverDescription>
    </PopoverHeader>
  </PopoverContent>
</Popover>
```

## 構成

`Popover` を構築するには、次の構成を使用します。

```text
Popover
├── PopoverTrigger
└── PopoverContent
```

## 基本

ヘッダー、タイトル、説明を含むシンプルなポップオーバー。

<ComponentPreview styleName="radix-nova" name="popover-basic" />

## 整列

`PopoverContent` の `align` プロップを使用して、水平方向の配置を制御します。

<ComponentPreview styleName="radix-nova" name="popover-alignments" />

## フォーム付き

内部にフォームフィールドを含むポップオーバー。

<ComponentPreview styleName="radix-nova" name="popover-form" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview styleName="radix-nova" name="popover-rtl" direction="rtl" />

## API リファレンス

詳細については、[Radix UI Popover](https://www.radix-ui.com/docs/primitives/components/popover#api-reference) ドキュメントを参照してください。
