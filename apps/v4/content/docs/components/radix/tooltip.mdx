---
title: ツールチップ
description: 要素がキーボード フォーカスを受け取るか、マウスでホバーされたときに、関連情報を表示するポップアップ。
base: radix
component: true
links:
  doc: https://www.radix-ui.com/docs/primitives/components/tooltip
  api: https://www.radix-ui.com/docs/primitives/components/tooltip#api-reference
---

<ComponentPreview styleName="radix-nova" name="tooltip-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

<Steps className="mb-0 pt-2">

<Step>次のコマンドを実行します。</Step>

```bash
npx shadcn@latest add tooltip
```

<Step>`TooltipProvider` をアプリのルートに追加します。</Step>

```tsx title="app/layout.tsx" showLineNumbers {1,7}
import { TooltipProvider } from "@/components/ui/tooltip"

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <TooltipProvider>{children}</TooltipProvider>
      </body>
    </html>
  )
}
```

</Steps>

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install radix-ui
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="tooltip"
  title="components/ui/tooltip.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

<Step>`TooltipProvider` をアプリのルートに追加します。</Step>

```tsx title="app/layout.tsx" showLineNumbers {1,7}
import { TooltipProvider } from "@/components/ui/tooltip"

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <TooltipProvider>{children}</TooltipProvider>
      </body>
    </html>
  )
}
```

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  Tooltip,
  TooltipContent,
  TooltipTrigger,
} from "@/components/ui/tooltip"
```

```tsx showLineNumbers
<Tooltip>
  <TooltipTrigger>Hover</TooltipTrigger>
  <TooltipContent>
    <p>Add to library</p>
  </TooltipContent>
</Tooltip>
```

## 構成

`Tooltip` を構築するには、次の構成を使用します。

```text
Tooltip
├── TooltipTrigger
└── TooltipContent
```

## サイド

ツールチップの位置を変更するには、`side` プロップを使用します。

<ComponentPreview styleName="radix-nova" name="tooltip-sides" />

## キーボード ショートカット付き

<ComponentPreview styleName="radix-nova" name="tooltip-keyboard" />

## 無効なボタン

無効になっているボタンをスパンで囲んでツールチップを表示します。

<ComponentPreview styleName="radix-nova" name="tooltip-disabled" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview styleName="radix-nova" name="tooltip-rtl" direction="rtl" />

## API リファレンス

[Radix Tooltip](https://www.radix-ui.com/docs/primitives/components/tooltip#api-reference) ドキュメントを参照してください。
