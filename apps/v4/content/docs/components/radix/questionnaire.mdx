---
title: アンケート
description: 単一選択、複数選択、自由入力、スキップ可能な質問に対応した複数ステップのアンケート。
base: radix
component: true
---

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-demo"
  previewClassName="min-h-[560px] p-4 sm:p-8"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">マニュアル</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add questionnaire
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install @shadcn/react
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="questionnaire"
  title="components/ui/questionnaire.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx
import {
  Questionnaire,
  QuestionnaireActions,
  QuestionnaireChoice,
  QuestionnaireChoices,
  QuestionnaireDescription,
  QuestionnaireError,
  QuestionnaireInput,
  QuestionnaireItem,
  QuestionnaireNext,
  QuestionnairePrevious,
  QuestionnaireProgress,
  QuestionnaireSkip,
  QuestionnaireSubmit,
  QuestionnaireTitle,
} from "@/components/ui/questionnaire"
```

```tsx
const items = [
  {
    name: "direction",
    required: true,
    prompt: "What should we prototype next?",
    description: "Choose a direction or write your own.",
    choices: [
      {
        value: "delegation",
        label: "Delegation",
        description: "Show how work moves to a specialist.",
      },
      {
        value: "questions",
        label: "Question prompts",
        description: "Show choices while the interface waits.",
      },
      { value: "both", label: "Both together" },
    ],
    input: { label: "Another answer", placeholder: "Type another answer…" },
  },
  {
    name: "detail",
    required: false,
    prompt: "How much detail should it include?",
    description: "Skip this if you are not sure yet.",
    choices: [
      { value: "focused", label: "Focused" },
      { value: "complete", label: "Complete flow" },
    ],
  },
] as const
```

コレクションを一度定義し、`Questionnaire` に渡すことで、進行状況・アクション・ショートカットをサーバーでレンダリングし、それを各パーツにマッピングします。

```tsx
<Questionnaire items={items} onSubmit={handleSubmit}>
  <QuestionnaireProgress />
  {items.map((question) => (
    <QuestionnaireItem
      key={question.name}
      name={question.name}
      required={question.required}
    >
      <QuestionnaireTitle>{question.prompt}</QuestionnaireTitle>
      <QuestionnaireDescription>
        {question.description}
      </QuestionnaireDescription>
      <QuestionnaireChoices>
        {question.choices.map((choice) => (
          <QuestionnaireChoice key={choice.value} value={choice.value}>
            <span className="font-medium">{choice.label}</span>
            {"description" in choice ? (
              <span className="text-muted-foreground">
                {choice.description}
              </span>
            ) : null}
          </QuestionnaireChoice>
        ))}
        {"input" in question ? (
          <QuestionnaireInput
            aria-label={question.input.label}
            placeholder={question.input.placeholder}
          />
        ) : null}
      </QuestionnaireChoices>
      <QuestionnaireError />
    </QuestionnaireItem>
  ))}
  <QuestionnaireActions>
    <QuestionnairePrevious />
    <QuestionnaireSkip />
    <QuestionnaireNext />
    <QuestionnaireSubmit />
  </QuestionnaireActions>
</Questionnaire>
```

```tsx
function handleSubmit(event: React.FormEvent<HTMLFormElement>) {
  event.preventDefault()
  const answers = new FormData(event.currentTarget)
  // answers.get("direction"), answers.getAll(...) for multiple items.
}
```

## 構成

```text
Questionnaire
├── QuestionnaireProgress
├── QuestionnaireItem
│   ├── QuestionnaireTitle
│   ├── QuestionnaireDescription
│   ├── QuestionnaireChoices
│   │   ├── QuestionnaireChoice
│   │   └── QuestionnaireInput
│   └── QuestionnaireError
└── QuestionnaireActions
    ├── QuestionnairePrevious
    ├── QuestionnaireSkip
    ├── QuestionnaireNext
    └── QuestionnaireSubmit
```

Questionnaire は、順序付けられた項目、アクティブな項目、回答の状態、検証、進行状況、ナビゲーションを管理します。それを含むページ、カード、ダイアログ、ドロワーは、クローズやキャンセルの動作、永続化、トランスポート、分岐を管理します。

## サーバーレンダリング

`items` を渡すと、アクティブな項目、進行状況、アクション、回答のショートカットがサーバーでレンダリングされます。完全な動作については、[headless Questionnaire](/docs/react/questionnaire) を参照してください。

## 複数選択

複数の固定回答を受け入れる項目には `multiple` を使用します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-multiple"
  previewClassName="min-h-[420px] p-4 sm:p-8"
/>

## 自由入力

ユーザーが別の回答を提供できる場合は、固定の選択肢を使用して `QuestionnaireInput` を作成します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-freeform"
  previewClassName="min-h-[420px] p-4 sm:p-8"
/>

## 明示的なスキップ

オプションの項目が意図的に未回答のままになっている可能性がある場合は、`QuestionnaireSkip` を追加します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-skip"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## ショートカット

`shortcuts` を使用して、各回答に文字または数字キーを割り当てます。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-shortcuts"
  previewClassName="min-h-[480px] p-4 sm:p-8"
/>

## カスタム検証

制御されたナビゲーションと Zod などの外部スキーマを組み合わせて、無効な項目に戻り、そのエラーを表示します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-validation"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## 制御

無効なステップに戻るなど、ホスト状態からアクティブな項目を制御します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-controlled"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## 再開

保存されたアクティブな項目とデフォルトの回答を復元し、変更をその保存された状態にリセットします。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-resume"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## 条件付き項目

ユーザーの以前の回答に該当しない項目を無効にします。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-conditional"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## ナビゲーション状態

アイテムのステータスを読み取り、無効なナビゲーションとカスタムアクションスタイルをオプトインします。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-navigation-state"
  previewClassName="min-h-[480px] p-4 sm:p-8"
/>

## カスタム進行状況

進行状況レンダリング状態を使用して、カスタム進行状況インジケーターを作成します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-progress"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## アニメーション付き項目

進行状況とナビゲーションを静止させたまま、アクティブな項目をアニメーション化します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-animated"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## カード

質問のタイトルと説明のセマンティックを維持しながら、カードスロットを使用してアンケートを作成します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-card"
  previewClassName="min-h-[560px] p-4 sm:p-8"
/>

## ダイアログ

キャンセルと閉じる操作をホスト側で管理しながら、ダイアログ内にアンケートを構成します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-dialog"
  previewClassName="min-h-[320px] p-4 sm:p-8"
/>

## アクセシビリティ

`QuestionnaireItem` は `fieldset` を、`QuestionnaireTitle` はその `legend` をレンダリングします。説明とアクティブなエラーは現在の項目に関連付けられ、無効な項目や回答コントロールには `aria-invalid` が付与されます。

固定の選択肢はネイティブのラジオボタンおよびチェックボックスの動作を保持します。進行状況は名前付きのプログレスバーとして公開され、ナビゲーションには実際のボタンが使用されます。非アクティブな項目とアクションは非表示かつ操作不可になります。ナビゲーションに成功すると新しくアクティブになった項目にフォーカスが移り、検証に失敗すると利用可能な回答コントロールにフォーカスが移ります。

`QuestionnaireInput` には、表示ラベル、`aria-label`、`aria-labelledby` のいずれかを使って必ずアクセシブルな名前を付けてください。プレースホルダーはラベルの代わりにはなりません。カスタムな構成のラベル付けと完全なキーボード操作については、[アンケートアクセシビリティガイド](/docs/react/questionnaire#accessibility) を参照してください。

## スタイルなし

`Questionnaire` の動作は `@shadcn/react` パッケージに由来します。独自のマークアップとスタイルでそのまま使用する方法については、@shadcn/react の [Questionnaire](/docs/react/questionnaire) を参照してください。

## API リファレンス

各パーツのプロパティ、データ属性、レンダリング状態は [@shadcn/react Questionnaire](/docs/react/questionnaire#api-reference) ページに記載されています。スタイル付きコンポーネントは、対応する非スタイルコンポーネントのプロパティを継承します。ナビゲーションコンポーネントは Button の `size` および `variant` プロパティも受け付けます。また、`QuestionnaireActions` はスタイル専用のレイアウトヘルパーです。
