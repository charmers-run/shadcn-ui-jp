---
title: 日付ピッカー
description: 範囲選択とプリセットに対応した日付ピッカー コンポーネント。
base: radix
component: true
---

<ComponentPreview styleName="radix-nova" name="date-picker-demo" />

## インストール

日付ピッカーは、`<Popover />` コンポーネントと `<Calendar />` コンポーネントの組み合わせを使用して構築されます。

[Popover](/docs/components/radix/popover#installation) および [Calendar](/docs/components/radix/calendar#installation) コンポーネントのインストール手順を参照してください。

## 使い方

```tsx showLineNumbers title="components/example-date-picker.tsx"
"use client"

import * as React from "react"
import { cn } from "cn"
import { format } from "date-fns"
import { Calendar as CalendarIcon } from "lucide-react"

import { Button } from "@/components/ui/button"
import { Calendar } from "@/components/ui/calendar"
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover"

export function DatePickerDemo() {
  const [date, setDate] = React.useState<Date>()

  return (
    <Popover>
      <PopoverTrigger asChild>
        <Button
          variant="outline"
          data-empty={!date}
          className="w-[280px] justify-start text-left font-normal data-[empty=true]:text-muted-foreground"
        >
          <CalendarIcon />
          {date ? format(date, "PPP") : <span>Pick a date</span>}
        </Button>
      </PopoverTrigger>
      <PopoverContent className="w-auto p-0">
        <Calendar mode="single" selected={date} onSelect={setDate} />
      </PopoverContent>
    </Popover>
  )
}
```

詳細については、[React DayPicker](https://react-day-picker.js.org) のドキュメントを参照してください。

## 構成

日付ピッカーは `Popover` と `Calendar` から構成されます（`DatePicker` というルート コンポーネントはありません）。

```text
Popover
├── PopoverTrigger
└── PopoverContent
    └── Calendar
```

## 基本

基本的な日付ピッカー コンポーネント。

<ComponentPreview styleName="radix-nova" name="date-picker-basic" />

## 範囲ピッカー

日付の範囲を選択するための日付ピッカー コンポーネント。

<ComponentPreview styleName="radix-nova" name="date-picker-range" />

## 生年月日

生年月日を選択するための日付ピッカー コンポーネント。このコンポーネントには、日付と月を選択するためのドロップダウン キャプション レイアウトが含まれています。

<ComponentPreview styleName="radix-nova" name="date-picker-dob" />

## 入力

日付を選択するための入力フィールド付き日付ピッカーコンポーネントです。

<ComponentPreview styleName="radix-nova" name="date-picker-input" />

## タイムピッカー

時刻を選択するための時刻入力フィールド付き日付ピッカーコンポーネントです。

<ComponentPreview styleName="radix-nova" name="date-picker-time" />

## 自然言語ピッカー

このコンポーネントは `chrono-node` ライブラリを使用して自然言語の日付を解析します。

<ComponentPreview styleName="radix-nova" name="date-picker-natural-language" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview
  styleName="radix-nova"
  name="date-picker-rtl"
  direction="rtl"
/>
