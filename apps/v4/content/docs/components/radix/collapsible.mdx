---
title: 折りたたみ可能
description: パネルを展開/縮小する対話型コンポーネント。
base: radix
component: true
featured: true
links:
  doc: https://www.radix-ui.com/docs/primitives/components/collapsible
  api: https://www.radix-ui.com/docs/primitives/components/collapsible#api-reference
---

<ComponentPreview
  styleName="radix-nova"
  name="collapsible-demo"
  align="start"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add collapsible
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install radix-ui
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="collapsible"
  title="components/ui/collapsible.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import {
  Collapsible,
  CollapsibleContent,
  CollapsibleTrigger,
} from "@/components/ui/collapsible"
```

```tsx showLineNumbers
<Collapsible>
  <CollapsibleTrigger>Can I use this in my project?</CollapsibleTrigger>
  <CollapsibleContent>
    Yes. Free to use for personal and commercial projects. No attribution
    required.
  </CollapsibleContent>
</Collapsible>
```

## 構成

次の構成を使用して `Collapsible` を作成します。

```text
Collapsible
├── CollapsibleTrigger
└── CollapsibleContent
```

## 制御状態

`open` および `onOpenChange` プロパティを使用して状態を制御します。

```tsx showLineNumbers
import * as React from "react"

export function Example() {
  const [open, setOpen] = React.useState(false)

  return (
    <Collapsible open={open} onOpenChange={setOpen}>
      <CollapsibleTrigger>Toggle</CollapsibleTrigger>
      <CollapsibleContent>Content</CollapsibleContent>
    </Collapsible>
  )
}
```

## 基本

<ComponentPreview
  styleName="radix-nova"
  name="collapsible-basic"
  align="start"
/>

## 設定パネル

追加の設定を表示するには、トリガー ボタンを使用します。

<ComponentPreview styleName="radix-nova" name="collapsible-settings" />

## ファイル ツリー

ネストされた折りたたみ可能コンポーネントを使用してファイル ツリーを構築します。

<ComponentPreview
  styleName="radix-nova"
  name="collapsible-file-tree"
  previewClassName="h-[36rem]"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="radix-nova"
  name="collapsible-rtl"
  direction="rtl"
  align="start"
/>

## API リファレンス

詳細については、[Radix UI](https://www.radix-ui.com/docs/primitives/components/collapsible#api-reference) ドキュメントを参照してください。
