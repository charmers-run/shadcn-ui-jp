---
title: キーボード入力
description: キーボードからのテキスト ユーザー入力を表示するために使用されます。
base: radix
component: true
---

<ComponentPreview styleName="radix-nova" name="kbd-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add kbd
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="kbd"
  title="components/ui/kbd.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx
import { Kbd } from "@/components/ui/kbd"
```

```tsx
<Kbd>Ctrl</Kbd>
```

## 構成

次の構成を使用して `Kbd` および `KbdGroup` を構築します。

```text
Kbd
KbdGroup
├── Kbd
└── Kbd
```

## グループ

`KbdGroup` コンポーネントを使用して、キーボード キーをグループ化します。

<ComponentPreview styleName="radix-nova" name="kbd-group" />

## ボタン

`Button` コンポーネント内で `Kbd` コンポーネントを使用して、ボタン内にキーボード キーを表示します。

<ComponentPreview styleName="radix-nova" name="kbd-button" />

## ツールチップ

`Tooltip` コンポーネント内で `Kbd` コンポーネントを使用すると、キーボード キーでツールチップを表示できます。

<ComponentPreview styleName="radix-nova" name="kbd-tooltip" />

## 入力グループ

`InputGroupAddon` コンポーネント内で `Kbd` コンポーネントを使用すると、入力グループ内にキーボード キーを表示できます。

<ComponentPreview styleName="radix-nova" name="kbd-input-group" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview styleName="radix-nova" name="kbd-rtl" direction="rtl" />

## API リファレンス

### Kbd

`Kbd` コンポーネントを使用して、キーボードのキーを表示します。

|プロパティ |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | `` |

```tsx
<Kbd>Ctrl</Kbd>
```

### KbdGroup

`KbdGroup` コンポーネントを使用して、`Kbd` コンポーネントをグループ化します。

|プロパティ |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | `` |

```tsx
<KbdGroup>
  <Kbd>Ctrl</Kbd>
  <Kbd>B</Kbd>
</KbdGroup>
```
