---
title: カード
description: ヘッダー、コンテンツ、フッターを含むカードを表示します。
base: radix
component: true
---

<ComponentPreview
  name="card-demo"
  styleName="radix-nova"
  previewClassName="h-[30rem]"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add card
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="card"
  title="components/ui/card.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import {
  Card,
  CardAction,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "@/components/ui/card"
```

```tsx showLineNumbers
<Card>
  <CardHeader>
    <CardTitle>Card Title</CardTitle>
    <CardDescription>Card Description</CardDescription>
    <CardAction>Card Action</CardAction>
  </CardHeader>
  <CardContent>
    <p>Card Content</p>
  </CardContent>
  <CardFooter>
    <p>Card Footer</p>
  </CardFooter>
</Card>
```

## 構成

`Card` を構築するには、次の構成を使用します。

```text
Card
├── CardHeader
│   ├── CardTitle
│   ├── CardDescription
│   └── CardAction
├── CardContent
└── CardFooter
```

## サイズ

`size="sm"` プロップを使用して、カードのサイズを小さく設定します。小さいサイズのバリエーションでは、より狭い間隔が使用されます。

<ComponentPreview
  styleName="radix-nova"
  name="card-small"
  previewClassName="h-96"
/>

## 間隔

`size` プロップに加えて、`--card-spacing` CSS 変数を使用して、セクション間の間隔とカード パーツの差し込みを制御できます。

<ComponentPreview
  styleName="radix-nova"
  name="card-spacing"
  previewClassName="h-[34rem]"
/>

`-mx-(--card-spacing)` で負のマージンを使用すると、コンテンツをカードの差し込み口と揃えたまま端から端まで移動させることができます。端から端までのコンテンツがフッターの上にある場合は、`CardContent` で `-mb-(--card-spacing)` を使用してセクションのギャップを削除します。

<ComponentPreview
  styleName="radix-nova"
  name="card-edge-to-edge"
  previewClassName="h-[24rem]"
/>

## 画像

画像付きのカードを作成するには、カード ヘッダーの前に画像を追加します。

<ComponentPreview
  styleName="radix-nova"
  name="card-image"
  previewClassName="h-[32rem]"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="radix-nova"
  name="card-rtl"
  direction="rtl"
  previewClassName="h-[30rem]"
/>

## API リファレンス

### カード

`Card` コンポーネントは、カード コンテンツのルート コンテナーです。

|プロップ |タイプ |デフォルト |
| ----------- | ------------------- | ----------- |
| `size` | `"default" \| "sm"` | `"default"` |
| `className` | `string` | - |

### CardHeader

`CardHeader` コンポーネントは、タイトル、説明、およびオプションのアクションに使用されます。

|プロップ |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | - |

### CardTitle

`CardTitle` コンポーネントはカードのタイトルに使用されます。

|プロップ |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | - |

### CardDescription

`CardDescription` コンポーネントは、タイトルの下のヘルパー テキストに使用されます。

|プロップ |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | - |

### CardAction

`CardAction` コンポーネントは、ヘッダーの右上にコンテンツ (ボタンやバッジなど) を配置します。

|プロップ |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | - |

### CardContent

`CardContent` コンポーネントはメイン カード本体に使用されます。

|プロップ |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | - |

### カードフッター

`CardFooter` コンポーネントは、カード下部のアクションと二次コンテンツに使用されます。

|プロップ |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | - |

## 変更履歴

### 間隔変数

`Card` コンポーネントの以前のバージョンからアップグレードする場合は、`--card-spacing` 変数を使用するために次の更新を適用する必要があります。

<Steps>

<Step>カードのルート間隔クラスを更新します。</Step>

ハードコーディングされたギャップと垂直パディングを `--card-spacing` に置き換え、ルートにデフォルトと小さいサイズの値を設定します。

```diff
  className={cn(
-   "group/card flex flex-col gap-4 overflow-hidden rounded-xl bg-card py-4 text-sm text-card-foreground ring-1 ring-foreground/10 has-data-[slot=card-footer]:pb-0 has-[>img:first-child]:pt-0 data-[size=sm]:gap-3 data-[size=sm]:py-3 data-[size=sm]:has-data-[slot=card-footer]:pb-0 *:[img:first-child]:rounded-t-xl *:[img:last-child]:rounded-b-xl",
+   "group/card flex flex-col gap-(--card-spacing) overflow-hidden rounded-xl bg-card py-(--card-spacing) text-sm text-card-foreground ring-1 ring-foreground/10 [--card-spacing:--spacing(4)] has-data-[slot=card-footer]:pb-0 has-[>img:first-child]:pt-0 data-[size=sm]:[--card-spacing:--spacing(3)] data-[size=sm]:has-data-[slot=card-footer]:pb-0 *:[img:first-child]:rounded-t-xl *:[img:last-child]:rounded-b-xl",
    className
  )}
```

<Step>CardHeader 間隔クラスを更新します。</Step>

水平方向のパディングと境界線の間隔を共有変数で置き換えます。

```diff
  className={cn(
-   "group/card-header @container/card-header grid auto-rows-min items-start gap-1 rounded-t-xl px-4 group-data-[size=sm]/card:px-3 has-data-[slot=card-action]:grid-cols-[1fr_auto] has-data-[slot=card-description]:grid-rows-[auto_auto] [.border-b]:pb-4 group-data-[size=sm]/card:[.border-b]:pb-3",
+   "group/card-header @container/card-header grid auto-rows-min items-start gap-1 rounded-t-xl px-(--card-spacing) has-data-[slot=card-action]:grid-cols-[1fr_auto] has-data-[slot=card-description]:grid-rows-[auto_auto] [.border-b]:pb-(--card-spacing)",
    className
  )}
```

<Step>CardContent および CardFooter 間隔クラスを更新します。</Step>

コンテンツ インセットとフッター パディングには `--card-spacing` を使用します。

```diff
  function CardContent({ className, ...props }: React.ComponentProps<"div">) {
    return (
      <div
        data-slot="card-content"
-       className={cn("px-4 group-data-[size=sm]/card:px-3", className)}
+       className={cn("px-(--card-spacing)", className)}
        {...props}
      />
    )
  }
```

```diff
  className={cn(
-   "flex items-center rounded-b-xl border-t bg-muted/50 p-4 group-data-[size=sm]/card:p-3",
+   "flex items-center rounded-b-xl border-t bg-muted/50 p-(--card-spacing)",
    className
  )}
```

</Steps>

これらの変更を適用した後、任意のプロパティ クラスを使用して `Card` に `--card-spacing` を設定することで、カードの間隔をカスタマイズできます。

```tsx
function Example() {
  return <Card className="[--card-spacing:--spacing(6)]">...</Card>
}
```
