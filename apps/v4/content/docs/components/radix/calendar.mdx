---
title: カレンダー
description: 日付または日付範囲を選択できるカレンダー コンポーネント。
base: radix
component: true
links:
  doc: https://react-day-picker.js.org
---

<ComponentPreview
  styleName="radix-nova"
  name="calendar-demo"
  previewClassName="h-96"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add calendar
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-day-picker date-fns
```

<Step>`Button` コンポーネントをプロジェクトに追加します。</Step>

`Calendar` コンポーネントは `Button` コンポーネントを使用します。プロジェクトにインストールされていることを確認してください。

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="calendar"
  title="components/ui/calendar.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import { Calendar } from "@/components/ui/calendar"
```

```tsx showLineNumbers
const [date, setDate] = React.useState<Date | undefined>(new Date())

return (
  <Calendar
    mode="single"
    selected={date}
    onSelect={setDate}
    className="rounded-lg border"
  />
)
```

詳細については、[React DayPicker](https://react-day-picker.js.org) ドキュメントを参照してください。

## 概要

`Calendar` コンポーネントは、[React DayPicker](https://react-day-picker.js.org) の上に構築されています。

## 日付ピッカー

`<Calendar>` コンポーネントを使用して日付ピッカーを構築できます。詳細については、[日付ピッカー](/docs/components/radix/date-picker) ページを参照してください。

## ペルシア語/ヒジュラ語/ジャラリ語カレンダー

ペルシャ暦を使用するには、`components/ui/calendar.tsx` を編集し、`react-day-picker` を `react-day-picker/persian` に置き換えます。

```diff
- import { DayPicker } from "react-day-picker"
+ import { DayPicker } from "react-day-picker/persian"
```

<ComponentPreview
  styleName="radix-nova"
  name="calendar-hijri"
  title="Persian / Hijri / Jalali Calendar"
  description="ペルシャ暦です。"
  previewClassName="h-[400px]"
/>

## 選択された日付 (タイムゾーンあり)

Calendar コンポーネントは `timeZone` プロパティを受け入れ、ユーザーのローカル タイムゾーンで日付が表示および選択されるようにします。

```tsx showLineNumbers
export function CalendarWithTimezone() {
  const [date, setDate] = React.useState<Date | undefined>(undefined)
  const [timeZone, setTimeZone] = React.useState<string | undefined>(undefined)

  React.useEffect(() => {
    setTimeZone(Intl.DateTimeFormat().resolvedOptions().timeZone)
  }, [])

  return (
    <Calendar
      mode="single"
      selected={date}
      onSelect={setDate}
      timeZone={timeZone}
    />
  )
}
```

**注意:** 選択した日付のオフセットに気付いた場合 (たとえば、20 日を選択すると 19 日が強調表示されます)、`timeZone` プロパティがユーザーのローカル タイムゾーンに設定されていることを確認してください。

**クライアント側で行う理由** サーバー側レンダリングとの互換性を確保するために、`useEffect` 内の `Intl.DateTimeFormat().resolvedOptions().timeZone` を使用してタイムゾーンが検出されます。サーバーとクライアントのタイムゾーンが異なる可能性があるため、レンダリング中にタイムゾーンを検出するとハイドレーションの不一致が発生します。

## 基本

基本的なカレンダー コンポーネント。カレンダーのスタイルには `className="rounded-lg border"` を使用しました。

<ComponentPreview
  styleName="radix-nova"
  name="calendar-basic"
  previewClassName="h-96"
/>

## 範囲カレンダー

`mode="range"` プロパティを使用して範囲選択を有効にします。

<ComponentPreview
  styleName="radix-nova"
  name="calendar-range"
  previewClassName="h-[36rem] md:h-96"
/>

## 月と年のセレクター

月と年のドロップダウンを表示するには、`captionLayout="dropdown"` を使用します。

<ComponentPreview
  styleName="radix-nova"
  name="calendar-caption"
  previewClassName="h-96"
/>

## プリセット

<ComponentPreview
  styleName="radix-nova"
  name="calendar-presets"
  previewClassName="h-[650px]"
/>

## 日付と時刻のピッカー

<ComponentPreview
  styleName="radix-nova"
  name="calendar-time"
  previewClassName="h-[600px]"
/>

## 予約済みの日付

<ComponentPreview
  styleName="radix-nova"
  name="calendar-booked-dates"
  previewClassName="h-96"
/>

## カスタムセルサイズ

<ComponentPreview
  styleName="radix-nova"
  name="calendar-custom-days"
  title="カスタムセルサイズ"
  description="応答性に優れたカスタムセルサイズのカレンダー。"
  className="**:[.preview]:h-[560px]"
/>

CSS 変数 `--cell-size` を使用して、カレンダーのセルのサイズをカスタマイズできます。ブレークポイント固有の値を使用して応答性を高めることもできます。

```tsx showLineNumbers
<Calendar
  mode="single"
  selected={date}
  onSelect={setDate}
  className="rounded-lg border [--cell-size:--spacing(11)] md:[--cell-size:--spacing(12)]"
/>
```

または、固定値を使用します。

```tsx showLineNumbers
<Calendar
  mode="single"
  selected={date}
  onSelect={setDate}
  className="rounded-lg border [--cell-size:2.75rem] md:[--cell-size:3rem]"
/>
```

## 週番号

週番号を表示するには `showWeekNumber` を使用します。

<ComponentPreview
  styleName="radix-nova"
  name="calendar-week-numbers"
  previewClassName="h-96"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

ペルシア語/ヒジュラ語/ジャラリ語カレンダーを有効にする方法については、[ヒジュラ語ガイド](#persian--hijri--jalali-calendar)も参照してください。

<ComponentPreview
  styleName="radix-nova"
  name="calendar-rtl"
  direction="rtl"
  previewClassName="h-96"
/>

RTL を使用する場合は、`react-day-picker/locale` からロケールをインポートし、`locale` と `dir` の両方のプロパティを Calendar コンポーネントに渡します。

```tsx showLineNumbers
import { arSA } from "react-day-picker/locale"

;<Calendar
  mode="single"
  selected={date}
  onSelect={setDate}
  locale={arSA}
  dir="rtl"
/>
```

## API リファレンス

`Calendar` コンポーネントの詳細については、[React DayPicker](https://react-day-picker.js.org) ドキュメントを参照してください。

## 変更履歴

### RTL サポート

`Calendar` コンポーネントの以前のバージョンからアップグレードする場合は、ロケールサポートを追加するために次の更新を適用する必要があります。

<Steps>

<Step>`Locale` 型をインポートします。</Step>

`react-day-picker` からのインポートに `Locale` を追加します。

```diff
  import {
    DayPicker,
    getDefaultClassNames,
    type DayButton,
+   type Locale,
  } from "react-day-picker"
```

<Step>`locale` プロパティを Calendar コンポーネントに追加します。</Step>

`locale` プロパティをコンポーネントのプロパティに追加します。

```diff
  function Calendar({
    className,
    classNames,
    showOutsideDays = true,
    captionLayout = "label",
    buttonVariant = "ghost",
+   locale,
    formatters,
    components,
    ...props
  }: React.ComponentProps<typeof DayPicker> & {
    buttonVariant?: React.ComponentProps<typeof Button>["variant"]
  }) {
```

<Step>`locale` を DayPicker に渡します。</Step>

`locale` プロパティを `DayPicker` コンポーネントに渡します。

```diff
    <DayPicker
      showOutsideDays={showOutsideDays}
      className={cn(...)}
      captionLayout={captionLayout}
+     locale={locale}
      formatters={{
        formatMonthDropdown: (date) =>
-         date.toLocaleString("default", { month: "short" }),
+         date.toLocaleString(locale?.code, { month: "short" }),
        ...formatters,
      }}
```

<Step>ロケールを受け入れるように CalendarDayButton を更新します。</Step>

`CalendarDayButton` コンポーネントのシグネチャを更新し、`locale` を渡します。

```diff
  function CalendarDayButton({
    className,
    day,
    modifiers,
+   locale,
    ...props
- }: React.ComponentProps<typeof DayButton>) {
+ }: React.ComponentProps<typeof DayButton> & { locale?: Partial<Locale> }) {
```

<Step>CalendarDayButton の日付書式を更新します。</Step>

日付書式設定で `locale?.code` を使用します。

```diff
    <Button
      variant="ghost"
      size="icon"
-     data-day={day.date.toLocaleDateString()}
+     data-day={day.date.toLocaleDateString(locale?.code)}
      ...
    />
```

<Step>ロケールを DayButton コンポーネントに渡します。</Step>

`DayButton` コンポーネントの使用法を更新して、`locale` prop を渡します。

```diff
      components={{
        ...
-       DayButton: CalendarDayButton,
+       DayButton: ({ ...props }) => (
+         <CalendarDayButton locale={locale} {...props} />
+       ),
        ...
      }}
```

<Step>RTL 対応の CSS クラスを更新します。</Step>

RTL サポートを向上させるために、方向性クラスを論理プロパティに置き換えます。

```diff
  // In the day classNames:
- [&:last-child[data-selected=true]_button]:rounded-r-(--cell-radius)
+ [&:last-child[data-selected=true]_button]:rounded-e-(--cell-radius)
- [&:nth-child(2)[data-selected=true]_button]:rounded-l-(--cell-radius)
+ [&:nth-child(2)[data-selected=true]_button]:rounded-s-(--cell-radius)
- [&:first-child[data-selected=true]_button]:rounded-l-(--cell-radius)
+ [&:first-child[data-selected=true]_button]:rounded-s-(--cell-radius)

  // In range_start classNames:
- rounded-l-(--cell-radius) ... after:right-0
+ rounded-s-(--cell-radius) ... after:end-0

  // In range_end classNames:
- rounded-r-(--cell-radius) ... after:left-0
+ rounded-e-(--cell-radius) ... after:start-0

  // In CalendarDayButton className:
- data-[range-end=true]:rounded-r-(--cell-radius)
+ data-[range-end=true]:rounded-e-(--cell-radius)
- data-[range-start=true]:rounded-l-(--cell-radius)
+ data-[range-start=true]:rounded-s-(--cell-radius)
```

</Steps>

これらの変更を適用すると、`locale` プロパティを使用してロケール固有の書式を指定できるようになります。

```tsx
import { enUS } from "react-day-picker/locale"

;<Calendar mode="single" selected={date} onSelect={setDate} locale={enUS} />
```
