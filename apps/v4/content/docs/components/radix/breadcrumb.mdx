---
title: ブレッドクラム
description: リンクの階層を使用して、現在のリソースへのパスを表示します。
base: radix
component: true
---

<ComponentPreview
  styleName="radix-nova"
  name="breadcrumb-demo"
  previewClassName="p-2"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add breadcrumb
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  styleName="radix-nova"
  name="breadcrumb"
  title="components/ui/breadcrumb.tsx"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  Breadcrumb,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbList,
  BreadcrumbPage,
  BreadcrumbSeparator,
} from "@/components/ui/breadcrumb"
```

```tsx showLineNumbers
<Breadcrumb>
  <BreadcrumbList>
    <BreadcrumbItem>
      <BreadcrumbLink href="/">Home</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator />
    <BreadcrumbItem>
      <BreadcrumbLink href="/components">Components</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator />
    <BreadcrumbItem>
      <BreadcrumbPage>Breadcrumb</BreadcrumbPage>
    </BreadcrumbItem>
  </BreadcrumbList>
</Breadcrumb>
```

## 構成

次の構成を使用して、`Breadcrumb` を構築します。

```text
Breadcrumb
└── BreadcrumbList
    ├── BreadcrumbItem
    │   └── BreadcrumbLink
    ├── BreadcrumbSeparator
    ├── BreadcrumbItem
    │   └── BreadcrumbLink
    ├── BreadcrumbSeparator
    └── BreadcrumbItem
        └── BreadcrumbPage
```

## 基本

ホーム リンクとコンポーネント リンクを含む基本的なブレッドクラム。

<ComponentPreview styleName="radix-nova" name="breadcrumb-basic" />

## カスタム区切り文字

カスタム コンポーネントを `<BreadcrumbSeparator />` の `children` として使用して、カスタム セパレーターを作成します。

<ComponentPreview styleName="radix-nova" name="breadcrumb-separator" />

## ドロップダウン

`<BreadcrumbItem />` を `<DropdownMenu />` と組み合わせて、パンくずリストにドロップダウンを作成できます。

<ComponentPreview styleName="radix-nova" name="breadcrumb-dropdown" />

## 折りたたまれた

ブレッドクラムが長すぎる場合に折りたたまれた状態を表示する `<BreadcrumbEllipsis />` コンポーネントを提供します。

<ComponentPreview
  styleName="radix-nova"
  name="breadcrumb-ellipsis"
  previewClassName="p-2"
/>

## リンクコンポーネント

ルーティング ライブラリのカスタム リンク コンポーネントを使用するには、`<BreadcrumbLink />` で `asChild` プロパティを使用できます。

<ComponentPreview styleName="radix-nova" name="breadcrumb-link" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="radix-nova"
  name="breadcrumb-rtl"
  direction="rtl"
  previewClassName="p-2"
/>

## API リファレンス

### ブレッドクラム

`Breadcrumb` コンポーネントは、すべてのブレッドクラム コンポーネントをラップするルート ナビゲーション要素です。

|小道具 |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | - |

### ブレッドクラムリスト

`BreadcrumbList` コンポーネントは、ブレッドクラム項目の順序付きリストを表示します。

|小道具 |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | - |

### ブレッドクラム項目

`BreadcrumbItem` コンポーネントは、個々のブレッドクラム項目をラップします。

|小道具 |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | - |

### ブレッドクラムリンク

`BreadcrumbLink` コンポーネントは、パンくずリストにクリック可能なリンクを表示します。

|小道具 |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | - |

### ブレッドクラムページ

`BreadcrumbPage` コンポーネントは、ブレッドクラムに現在のページを表示します (クリック不可)。

|小道具 |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | - |

### ブレッドクラム区切り文字

`BreadcrumbSeparator` コンポーネントは、ブレッドクラム項目間の区切り文字を表示します。カスタムの子要素を渡すことで、デフォルトの区切りアイコンをオーバーライドできます。

|小道具 |タイプ |デフォルト |
| ----------- | ----------------- | ------- |
| `children` | `React.ReactNode` | - |
| `className` | `string`          | -       |

### ブレッドクラム省略記号

`BreadcrumbEllipsis` コンポーネントは、折りたたまれたブレッドクラム項目の省略記号インジケーターを表示します。

|小道具 |タイプ |デフォルト |
| ----------- | -------- | ------- |
| `className` | `string` | - |
