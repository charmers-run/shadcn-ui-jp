---
title: タブ
description: タブパネルと呼ばれるコンテンツのレイヤー化されたセクションを、一度に1つ表示するセット。
base: radix
component: true
links:
  doc: https://www.radix-ui.com/docs/primitives/components/tabs
  api: https://www.radix-ui.com/docs/primitives/components/tabs#api-reference
---

<ComponentPreview
  styleName="radix-nova"
  name="tabs-demo"
  previewClassName="h-96"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add tabs
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install radix-ui
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="tabs"
  title="components/ui/tabs.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
```

```tsx showLineNumbers
<Tabs defaultValue="account" className="w-[400px]">
  <TabsList>
    <TabsTrigger value="account">Account</TabsTrigger>
    <TabsTrigger value="password">Password</TabsTrigger>
  </TabsList>
  <TabsContent value="account">Make changes to your account here.</TabsContent>
  <TabsContent value="password">Change your password here.</TabsContent>
</Tabs>
```

## 構成

次の構成を使用して、`Tabs` を構築します。

```text
Tabs
├── TabsList
│   ├── TabsTrigger
│   └── TabsTrigger
├── TabsContent
└── TabsContent
```

## 行

ライン スタイルにするには、`TabsList` に `variant="line"` プロパティを使用します。

<ComponentPreview styleName="radix-nova" name="tabs-line" />

## 垂直

垂直タブにするには、`orientation="vertical"` を使用します。

<ComponentPreview styleName="radix-nova" name="tabs-vertical" />

## 無効

<ComponentPreview styleName="radix-nova" name="tabs-disabled" />

## アイコン

<ComponentPreview styleName="radix-nova" name="tabs-icons" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview styleName="radix-nova" name="tabs-rtl" direction="rtl" />

## API リファレンス

詳細については、[Radix UI Tabs](https://www.radix-ui.com/docs/primitives/components/tabs#api-reference) ドキュメントを参照してください。
