---
title: ラジオグループ
description: ラジオボタンと呼ばれる、同時に1つまで選択できるチェック可能なボタンのセット。
base: radix
component: true
links:
  doc: https://www.radix-ui.com/docs/primitives/components/radio-group
  api: https://www.radix-ui.com/docs/primitives/components/radio-group#api-reference
---

<ComponentPreview styleName="radix-nova" name="radio-group-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add radio-group
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install radix-ui
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="radio-group"
  title="components/ui/radio-group.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import { Label } from "@/components/ui/label"
import { RadioGroup, RadioGroupItem } from "@/components/ui/radio-group"
```

```tsx showLineNumbers
<RadioGroup defaultValue="option-one">
  <div className="flex items-center gap-3">
    <RadioGroupItem value="option-one" id="option-one" />
    <Label htmlFor="option-one">Option One</Label>
  </div>
  <div className="flex items-center gap-3">
    <RadioGroupItem value="option-two" id="option-two" />
    <Label htmlFor="option-two">Option Two</Label>
  </div>
</RadioGroup>
```

## 構成

次の構成を使用して、`RadioGroup` を構築します。

```text
RadioGroup
├── RadioGroupItem
└── RadioGroupItem
```

## 説明

`Field` コンポーネントを使用して、説明付きのラジオグループ項目を表示します。

<ComponentPreview styleName="radix-nova" name="radio-group-description" />

## 選択カード

`FieldLabel` を使用して `Field` 全体をラップし、クリック可能なカードスタイルの選択を行います。

<ComponentPreview styleName="radix-nova" name="radio-group-choice-card" />

## フィールドセット

`FieldSet` と `FieldLegend` を使用して、ラベルと説明付きでラジオ項目をグループ化します。

<ComponentPreview styleName="radix-nova" name="radio-group-fieldset" />

## 無効

`RadioGroupItem` で `disabled` プロパティを使用して、個々の項目を無効にします。

<ComponentPreview styleName="radix-nova" name="radio-group-disabled" />

## 無効な状態

`RadioGroupItem` では `aria-invalid` を、`Field` では `data-invalid` を使用して検証エラーを表示します。

<ComponentPreview styleName="radix-nova" name="radio-group-invalid" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview
  styleName="radix-nova"
  name="radio-group-rtl"
  direction="rtl"
/>

## API リファレンス

詳細については、[Radix UI Radio Group](https://www.radix-ui.com/docs/primitives/components/radio-group#api-reference) のドキュメントを参照してください。
