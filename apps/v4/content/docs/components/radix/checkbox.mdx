---
title: チェックボックス
description: ユーザーがチェック済みとチェックなしを切り替えることができるコントロール。
base: radix
component: true
links:
  doc: https://www.radix-ui.com/docs/primitives/components/checkbox
  api: https://www.radix-ui.com/docs/primitives/components/checkbox#api-reference
---

<ComponentPreview
  styleName="radix-nova"
  name="checkbox-demo"
  previewClassName="h-80"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add checkbox
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします:</Step>

```bash
npm install radix-ui
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="checkbox"
  title="components/ui/checkbox.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx
import { Checkbox } from "@/components/ui/checkbox"
```

```tsx
<Checkbox />
```

## チェックされた状態

非制御のチェックボックスには `defaultChecked` を使用し、状態を制御するには `checked` と
`onCheckedChange` を使用します。

```tsx showLineNumbers
import * as React from "react"

export function Example() {
  const [checked, setChecked] = React.useState(false)

  return <Checkbox checked={checked} onCheckedChange={setChecked} />
}
```

## 無効な状態

チェックボックスに `aria-invalid`、フィールド ラッパーに `data-invalid` を設定すると、
無効なスタイルが表示されます。

<ComponentPreview styleName="radix-nova" name="checkbox-invalid" />

## 基本

適切なレイアウトとラベル付けを行うには、チェックボックスを `Field` および `FieldLabel` と組み合わせます。

<ComponentPreview styleName="radix-nova" name="checkbox-basic" />

## 説明

ヘルパー テキストには `FieldContent` と `FieldDescription` を使用します。

<ComponentPreview styleName="radix-nova" name="checkbox-description" />

## 無効

`disabled` プロパティを使用して相互作用を防止し、無効なスタイルの `<Field>` コンポーネントに `data-disabled` 属性を追加します。

<ComponentPreview styleName="radix-nova" name="checkbox-disabled" />

## グループ

複数のフィールドを使用してチェックボックス リストを作成します。

<ComponentPreview styleName="radix-nova" name="checkbox-group" />

## テーブル

<ComponentPreview
  styleName="radix-nova"
  name="checkbox-table"
  previewClassName="p-4 md:p-8"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="radix-nova"
  name="checkbox-rtl"
  direction="rtl"
  previewClassName="h-80"
/>

## API リファレンス

詳細については、[Radix UI](https://www.radix-ui.com/docs/primitives/components/checkbox#api-reference) ドキュメントを参照してください。
