---
title: セレクト
description: ボタンで開き、ユーザーが選択できるオプションのリストを表示します。
base: radix
component: true
featured: true
links:
  doc: https://www.radix-ui.com/docs/primitives/components/select
  api: https://www.radix-ui.com/docs/primitives/components/select#api-reference
---

<ComponentPreview styleName="radix-nova" name="select-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add select
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install radix-ui
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="select"
  title="components/ui/select.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  Select,
  SelectContent,
  SelectGroup,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select"
```

```tsx showLineNumbers
<Select>
  <SelectTrigger className="w-[180px]">
    <SelectValue placeholder="Theme" />
  </SelectTrigger>
  <SelectContent>
    <SelectGroup>
      <SelectItem value="light">Light</SelectItem>
      <SelectItem value="dark">Dark</SelectItem>
      <SelectItem value="system">System</SelectItem>
    </SelectGroup>
  </SelectContent>
</Select>
```

## 構成

次の構成を使用して、`Select` を構築します。

```text
Select
├── SelectTrigger
│   └── SelectValue
└── SelectContent
    ├── SelectGroup
    │   ├── SelectLabel
    │   ├── SelectItem
    │   └── SelectItem
    ├── SelectSeparator
    └── SelectGroup
        ├── SelectLabel
        ├── SelectItem
        └── SelectItem
```

## トリガーとの位置合わせ

`SelectContent` の `position` プロパティを使用して、位置合わせを制御します。`position="item-aligned"`(デフォルト)の場合、選択した項目がトリガーの上に表示されるようにポップアップが配置されます。`position="popper"` の場合、ポップアップはトリガーの端に揃えられます。

<ComponentPreview styleName="radix-nova" name="select-align-item" />

## グループ

`SelectGroup`、`SelectLabel`、`SelectSeparator` を使用して項目をグループ化します。

<ComponentPreview styleName="radix-nova" name="select-groups" />

## スクロール可能

多くの項目を含み、スクロール可能なセレクト。

<ComponentPreview styleName="radix-nova" name="select-scrollable" />

## 無効

<ComponentPreview styleName="radix-nova" name="select-disabled" />

## 無効な状態

エラー状態を表示するには、`Field` コンポーネントに `data-invalid` 属性を追加し、`SelectTrigger` コンポーネントに `aria-invalid` 属性を追加します。

```tsx showLineNumbers /data-invalid/ /aria-invalid/
<Field data-invalid>
  <FieldLabel>Fruit</FieldLabel>
  <SelectTrigger aria-invalid>
    <SelectValue />
  </SelectTrigger>
</Field>
```

<ComponentPreview styleName="radix-nova" name="select-invalid" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview styleName="radix-nova" name="select-rtl" direction="rtl" />

## API リファレンス

詳細については、[Radix UI Select](https://www.radix-ui.com/docs/primitives/components/select#api-reference) のドキュメントを参照してください。
