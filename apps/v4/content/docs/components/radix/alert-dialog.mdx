---
title: アラートダイアログ
description: 重要なコンテンツでユーザーを中断し、応答を期待するモーダルダイアログ。
featured: true
base: radix
component: true
links:
  doc: https://www.radix-ui.com/primitives/docs/components/alert-dialog
  api: https://www.radix-ui.com/primitives/docs/components/alert-dialog#api-reference
---

<ComponentPreview
  name="alert-dialog-demo"
  styleName="radix-nova"
  previewClassName="h-56"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add alert-dialog
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install radix-ui
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="alert-dialog"
  title="components/ui/alert-dialog.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from "@/components/ui/alert-dialog"
```

```tsx showLineNumbers
<AlertDialog>
  <AlertDialogTrigger asChild>
    <Button variant="outline">Show Dialog</Button>
  </AlertDialogTrigger>
  <AlertDialogContent>
    <AlertDialogHeader>
      <AlertDialogTitle>Are you absolutely sure?</AlertDialogTitle>
      <AlertDialogDescription>
        This action cannot be undone. This will permanently delete your account
        from our servers.
      </AlertDialogDescription>
    </AlertDialogHeader>
    <AlertDialogFooter>
      <AlertDialogCancel>Cancel</AlertDialogCancel>
      <AlertDialogAction>Continue</AlertDialogAction>
    </AlertDialogFooter>
  </AlertDialogContent>
</AlertDialog>
```

## 構成

次の構成を使用して、`AlertDialog` を構築します。

```text
AlertDialog
├── AlertDialogTrigger
└── AlertDialogContent
    ├── AlertDialogHeader
    │   ├── AlertDialogMedia
    │   ├── AlertDialogTitle
    │   └── AlertDialogDescription
    └── AlertDialogFooter
        ├── AlertDialogCancel
        └── AlertDialogAction
```

## 基本

タイトル、説明、キャンセルボタンと続行ボタンを備えた基本的なアラートダイアログ。

<ComponentPreview
  name="alert-dialog-basic"
  styleName="radix-nova"
  previewClassName="h-56"
/>

## 小さい

`size="sm"` プロパティを使用して、アラートダイアログを小さくします。

<ComponentPreview
  name="alert-dialog-small"
  styleName="radix-nova"
  previewClassName="h-56"
/>

## メディア

`AlertDialogMedia` コンポーネントを使用して、アイコンや画像などのメディア要素をアラートダイアログに追加します。

<ComponentPreview
  name="alert-dialog-media"
  styleName="radix-nova"
  previewClassName="h-56"
/>

## メディア付きの小さいダイアログ

`size="sm"` プロパティを使用してアラートダイアログを小さくし、`AlertDialogMedia` コンポーネントを使用してアイコンや画像などのメディア要素をアラートダイアログに追加します。

<ComponentPreview
  name="alert-dialog-small-media"
  styleName="radix-nova"
  previewClassName="h-56"
/>

## 破壊的

`AlertDialogAction` コンポーネントを使用して、破壊的なアクションボタンをアラートダイアログに追加します。

<ComponentPreview
  name="alert-dialog-destructive"
  styleName="radix-nova"
  previewClassName="h-56"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="radix-nova"
  name="alert-dialog-rtl"
  direction="rtl"
  previewClassName="h-56"
/>

## API リファレンス

### サイズ

`AlertDialogContent` コンポーネントの `size` プロパティを使用して、アラートダイアログのサイズを制御します。次の値を受け入れます。

| プロパティ | タイプ | デフォルト |
| ------ | ------------------- | ----------- |
| `size` | `"default" \| "sm"` | `"default"` |

他のコンポーネントとそのプロパティの詳細については、[Radix UI ドキュメント](https://www.radix-ui.com/primitives/docs/components/alert-dialog#api-reference) を参照してください。
