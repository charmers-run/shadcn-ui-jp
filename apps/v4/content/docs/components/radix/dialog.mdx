---
title: ダイアログ
description: プライマリ ウィンドウまたは別のダイアログ ウィンドウに重ねて表示されるウィンドウで、その下のコンテンツは不活性になります。
featured: true
base: radix
component: true
links:
  doc: https://www.radix-ui.com/docs/primitives/components/dialog
  api: https://www.radix-ui.com/docs/primitives/components/dialog#api-reference
---

<ComponentPreview
  styleName="radix-nova"
  name="dialog-demo"
  description="A dialog for editing profile details."
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add dialog
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install radix-ui
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="dialog"
  title="components/ui/dialog.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog"
```

```tsx showLineNumbers
<Dialog>
  <DialogTrigger>Open</DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Are you absolutely sure?</DialogTitle>
      <DialogDescription>
        This action cannot be undone. This will permanently delete your account
        and remove your data from our servers.
      </DialogDescription>
    </DialogHeader>
  </DialogContent>
</Dialog>
```

## 構成

次の構成を使用して `Dialog` を構築します。

```text
Dialog
├── DialogTrigger
└── DialogContent
    ├── DialogHeader
    │   ├── DialogTitle
    │   └── DialogDescription
    └── DialogFooter
```

## カスタム閉じるボタン

デフォルトの閉じるコントロールを独自のボタンに置き換えます。

<ComponentPreview styleName="radix-nova" name="dialog-close-button" />

## 閉じるボタンなし

`showCloseButton={false}` を使用すると、閉じるボタンを非表示にできます。

<ComponentPreview styleName="radix-nova" name="dialog-no-close-button" />

## スティッキー フッター

コンテンツがスクロールしている間、アクションを表示したままにします。

<ComponentPreview styleName="radix-nova" name="dialog-sticky-footer" />

## スクロール可能なコンテンツ

長いコンテンツはヘッダーを表示したままスクロールできます。

<ComponentPreview styleName="radix-nova" name="dialog-scrollable-content" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview styleName="radix-nova" name="dialog-rtl" direction="rtl" />

## API リファレンス

詳細については、[Radix UI](https://www.radix-ui.com/docs/primitives/components/dialog#api-reference) のドキュメントを参照してください。
